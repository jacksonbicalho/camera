# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## O que é este projeto

Sistema de monitoramento residencial via RTSP. Cada câmera configurada tem três processos ffmpeg rodando em paralelo: um grava chunks MP4 em disco, outro gera segmentos HLS para visualização ao vivo e um terceiro detecta movimento por diff de frames. O frontend React é embutido no binário Go via `go:embed`.

## Fluxo de trabalho

O fluxo completo — XP/TDD, análise, story decomposta em tickets, code review automatizado por subagent, testes funcionais, estratégia de branches, CI/branch protection, slash commands, hooks, scripts de workflow e planejamento/corte de release — vive **inteiramente** em **[`docs/workflow.md`](docs/workflow.md)**, única fonte de detalhe procedural do projeto: nada aqui duplica o que está lá, então se este resumo e `docs/workflow.md` alguma vez divergirem, `docs/workflow.md` vale. **Leia esse arquivo por completo antes de qualquer trabalho de fluxo** (o hook `session-start` lembra a cada sessão) — releia se não tiver certeza de já conhecê-lo nesta sessão (sessão nova, contexto resumido, etc.).

O que precisa sobreviver mesmo sem reler o arquivo:
- Só **3 gates humanos** — G1 (Análise aprovada), G2 (História revisada), G3 (release). Entre eles, o ciclo roda sozinho, sem prompts ao navigator (exceções documentadas em `docs/workflow.md`).
- `master` e `develop` são protegidos — nunca commit/push direto; tudo via PR.
- G3 (PR `develop → master`) só com ok explícito do navigator.

## Comandos principais

### Backend (Go)

```bash
go test ./...                                         # todos os testes
go test ./internal/server/... -run TestLogin          # teste específico
make build                                            # binário local com versão git injetada
make run                                              # sobe Docker dev com live reload (dev-camera)
make all                                              # cross-compila para linux-amd64/arm64/arm e windows-amd64
make linux-amd64                                      # binário específico em dist/
make rpi                                              # alias para linux-arm64 (Raspberry Pi 3/4/5 64-bit)
./camera init                                         # wizard interativo → gera camera.yaml no diretório atual
./camera init --output /etc/camera/camera.yaml        # wizard → grava no caminho especificado
./camera version                                      # imprime versão, commit e data do build
```

### Frontend (`frontend/src/`)

SPA React/Vite/Tailwind. Páginas principais: `LoginPage` → `LiveViewPage` (landing/página principal, rota `/`, história `feat/liveview-customizavel` T7 — antes a landing era a `AllCamerasPage`, removida) → `LivePage` / `HistoryPage` / `VideoBrowserPage` / `RecordingsPage` / `ReportsPage` (ver "Páginas principais" abaixo). Seção de configurações em `/settings/*` com sidebar lateral (padrão GitHub Settings). Token JWT em `localStorage` (`auth.ts`). Em desenvolvimento, Vite faz proxy de `/api` e `/stream` para `localhost:8080`.

**Padrão: edição via rota dedicada.** Toda página com um modo de edição usa uma rota própria pra ele — nunca `useState` local nem `state` de navegação, que se perdem em reload/deep-link/botão voltar do navegador. Vale pra qualquer página com edição, dentro ou fora de `/settings` — não é mais uma escolha caso-a-caso "essa página tem deep-link útil?" (decisão explícita do navigator, história `feat/storage-view-edit-toggle`). Convenção de nome: `.../edit` pra config única (`/settings/preferences/storage/edit`, `/profile/edit`) ou `.../edit/:id` pra entidade (`/settings/cameras/edit/:id`, `/settings/users/edit/:id`). `editing` é sempre **derivado**, nunca guardado em estado — `useLocation().pathname === '<rota-de-edição>'` (ou `.startsWith(...)` quando a rota tem `:id`); "Editar" chama `navigate('<rota-de-edição>')`; salvar (sucesso) e cancelar chamam `navigate('<rota-de-visualização>')`.

Como a navegação entre as rotas de visualização/edição NÃO remonta o componente (mesmo elemento lazy montado em mais de uma rota), o form precisa lidar com isso de um jeito ou de outro. Dois casos: **(1) mount/unmount condicional** (`editing ? <Form/> : <Visualização/>`, o caso comum) — o form nasce do zero (`useState(() => initFromProps(...))`) toda vez que entra em edição, sem esforço extra (`CameraForm`, `UserForm`, `StorageSettingsPage`). **(2) form que fica montado o tempo todo**, alternando de conteúdo pela sub-rota sem desmontar o pai (`ProfilePage`, entre `/profile/edit` e `/profile/change-email`) — precisa reidratar/resetar estado manualmente ao entrar em edição; como `setState` direto dentro de `useEffect` é barrado pelo eslint (`react-hooks/set-state-in-effect`), isso é feito com o padrão "adjusting state during render" (`prevKey`/`setKey`, o mesmo já usado em `CameraViewTabs.tsx`), nunca um `useEffect` — ver `ProfilePage.tsx` (história `feat/profile-rotas-edit-change-email`) pro exemplo completo, incluindo o caso do deep-link onde os dados ainda não chegaram do fetch no momento em que a rota já bate.

Implementações do padrão: `/settings/users/edit/:id` (`UserDetailSettingsPage`), `/settings/preferences/storage/edit` (`StorageSettingsPage`), `/profile/edit` + `/profile/change-email` (`ProfilePage`, 2 rotas de edição independentes no mesmo componente). **Carve-out**: `/settings/cameras/:id` (`CameraDetailSettingsPage`) seguia esse padrão (`/settings/cameras/edit/:id`) até a história `refactor/camera-detail-secoes-aplicar`, quando o navigator pediu (com screenshot) que a página parasse de alternar entre visualização/edição — virou **sempre-editável, seção por seção**, cada uma com seu próprio "Aplicar" (mesmo padrão que "Detecção de movimento"/"Detector de objetos" já usavam nesta mesma página desde antes). A rota de edição foi removida; ver `CameraDetailSettingsPage`/`CameraCaptureSection`/`CameraRecordingSection`/`CameraTransmissionSection` abaixo.

**Todo componente/elemento da UI deve ter um `id` único e estável.** Botões, painéis, itens de navegação, abas, o ponteiro da timeline etc. recebem um `id` descritivo (ex: `sidebar-settings`, `events-panel`, `timeline-pointer`, `theme-mode-dark`). Facilita testes, automação e referência inequívoca em revisões — ao criar ou alterar um elemento, garanta o `id`.

**Design tokens e tema (`src/index.css` → `src/styles/`).** Base estrutural organizada como **tema → modo → valores**, dividida em arquivos importados pelo `index.css` (que é só `@import`, nesta ordem: `tailwindcss` → `styles/primitives.css` → `styles/themes/default.css` → `styles/themes/accents.css` → `styles/base.css`): **`primitives.css`** = `@theme` com tipografia + rampas cruas (gray/accents) base; **`themes/default.css`** = o tema default, com os papéis semânticos no `@theme` (modo dark) e o bloco `[data-mode="light"]` (override do claro + inversão da rampa + tints + logo) — o cabeçalho do arquivo documenta **como adicionar um tema** (`themes/<nome>.css` com `[data-theme="<nome>"]` e `[data-theme="<nome>"][data-mode="light"]`, + `ThemeContext` aplicando `data-theme`); **`themes/accents.css`** = o eixo de cor de destaque (ver "Color mode ≠ tema ≠ accent" abaixo); **`base.css`** = keyframes, `body` e cursor. Os tokens vivem em blocos `@theme` do Tailwind v4 (geram utilitários):
- **Tipografia**: escala `text-display/h1/h2/h3/h4/body/caption` (size + line-height) → use os utilitários (`text-h2`, `text-h4`…) em vez de tamanhos soltos (`text-lg`/`text-xs`).
- **Cor semântica**: papéis `bg-background`, `bg-surface`, `bg-surface-2`, `text-foreground`, `text-muted`, `text-faint`, `border-border`, `bg-primary`/`text-on-primary`, `bg-danger/success/warning`. Os valores no `@theme` são o **modo dark** (padrão); `[data-mode="light"]` sobrescreve **só** os tokens semânticos que mudam (accents seguem vívidos). A migração das cores cruas (`bg-gray-900`/`text-white`) para esses papéis é **incremental** — boa parte do app ainda usa as classes Tailwind diretas, com o bloco legado `[data-mode="light"]` remapeando a rampa de cinzas.

**Exceção deliberada: MUI (`@mui/material`/`@mui/x-date-pickers`/`@emotion/*`).** O app é 100% Tailwind + `react-day-picker` + `@radix-ui/react-slot` — **exceto** o `TimeRangeFilterPanel` do Histórico (dial de relógio `TimePicker`, pedido explícito do navigator, ver história `feat/historico-filtro-intervalo-horario`), que precisa do MUI X. `MuiThemeProvider` (`components/MuiThemeProvider.tsx`) mapeia o `mode` resolvido do `ThemeContext` do app e o token `--color-primary` pro tema do MUI — **não é montado globalmente em `App.tsx`**, só embrulha quem genuinamente usa MUI por baixo. Isso **não é um precedente geral**: não migrar outros componentes pra MUI só porque a dependência já está instalada.

**Color mode ≠ tema ≠ accent.** `dark`/`light`/`system` são **modos de cor**; o **tema** é a identidade (paleta + tipografia); **accent** é um **3º eixo independente** (cor de destaque — `--color-primary`/`--color-primary-strong`/`--color-ring`), ortogonal aos outros dois. `ThemeContext` (`contexts/ThemeContext.tsx`) expõe `mode`/`setMode`, `theme` (hoje só `'default'`) e `accent`/`setAccent` (`AccentColor`: `default|violet|teal|coral|amber`); aplica `data-mode` (resolvido dark/light) e `data-accent` no `<html>` — `data-accent` só é setado quando o accent não é `'default'` (`'default'` = sem override, fica com o azul base de `themes/default.css`, e mesmo assim é uma opção normal e selecionável em `AppearanceSettingsPage`, não um fallback invisível). Ambas as preferências persistem via `GET/PUT /api/me/preferences` → `user_settings` (chaves `theme`/`accent`). **Adicionar um accent** = novo bloco `[data-accent="<nome>"]` em `themes/accents.css` + entrada em `ACCENT_OPTIONS` (`AppearanceSettingsPage.tsx`) + `validAccents` (`internal/server/theme.go`). **Adicionar um tema** no futuro = novo conjunto de valores de tokens (sem refatorar componentes).

Rotas de settings por câmera seguem o padrão **seção antes do id** (`/settings/cameras/<seção>/:id`): `/settings/cameras/:id` (detalhes — sempre editável pro admin desde a história `refactor/camera-detail-secoes-aplicar`, sem rota de edição própria — ver "Padrão: edição via rota dedicada" acima), `/settings/cameras/zones/:id` (zonas de exclusão). `/settings/cameras/motion/:id` (detecção de movimento) foi removida na história `feat/camera-form-reshape` (T4) — deixou de ser rota/aba própria e virou sessão dentro de `/settings/cameras/:id` (ver `CameraSettingsTabs`/`CameraMotionSection` abaixo). `/settings/cameras/analysis/:id` (análise por câmera) teve o mesmo destino, mas em duas etapas: virou rota IA-primeiro (`/settings/analyses/:id`, fora do padrão seção-antes-do-id) na história `refactor/camera-tabs-para-sidebar-ia`, e depois voltou a ser câmera-primeiro — sem readotar uma rota própria — na história `refactor/mover-analise-para-cadastro-camera`, virando sessão dentro de `/settings/cameras/:id` (`CameraAnalysisSection`, ver abaixo), igual a Motion. As rotas de API do backend mantêm o id antes do recurso (`/api/cameras/:id/motion/zones` etc.) — só as rotas do frontend usam seção-antes-do-id. State classification é a única que ainda foge desse padrão (mesma história `refactor/camera-tabs-para-sidebar-ia`, ver `CameraSettingsTabs`/Sidebar abaixo): saiu da navegação câmera-primeiro e virou `/settings/states/:id` (seção "Inteligência"), com landing própria (`/settings/states`) que resolve a 1ª câmera automaticamente (mesmo padrão de `/history`, ver `HistoryLandingPage` abaixo) e um `<select>` de câmera na própria página pra trocar sem voltar à landing.

**`Sidebar`** (`components/Sidebar.tsx`) — rail de navegação único do app (substituiu de vez o par legado `AppSidebar`/`AppLayout`, fechado nas histórias `refactor/unificar-configuracoes-settings` e `refactor/fechar-strangler-applayout`). Recolhido por padrão (só ícones, `w-14`), expande (`w-48`, com labels) — preferência persiste em `localStorage` (`ui-display-mode`). Seções sempre visíveis e empilhadas (`SidebarSection` — cabeçalho uppercase (`font-bold`/`text-muted`, desde a história `refactor/reorganizar-sidebar-ia` T2 — antes `font-semibold`/`text-faint`, opacidade 38% que deixava o cabeçalho mais apagado que os próprios itens de navegação abaixo dele, `text-muted` é 60%) + separador `border-t` acima, seguindo a lei da proximidade: mais espaço ACIMA do título do que abaixo dele); **sem** flyout/accordion atrás de um ícone "Configurações" (desenho anterior, abandonado na história `feat/reorganizar-menu-navegacao`). Não tem cabeçalho/logo próprio — isso mora na `TopBar` (ver abaixo); o rail começa direto pelo botão "Recolher menu" (1º item, ícone hambúrguer `Menu`, alterna `ui-display-mode`). De cima pra baixo: 1ª seção **sem cabeçalho visível** (só o item "Ao vivo" — chamado "Live View" até a história `feat/liveview-player-footer-limpeza`, quando o navigator pediu a tradução (título da página, `ConfirmDialog` de remover câmera e este label do rail, os três juntos pra não ficar link/página com nomes divergentes); a seção chamava "Eventos" enquanto também continha o sino `MotionNotificationsBell`; depois que ele migrou pra `TopBar`, história `feat/reorganizar-topbar-sidebar`, sobrou só um item, e um rótulo de seção pra item único virou ruído — passou por "Ao vivo" (como cabeçalho de seção, não como o próprio label) e depois foi removido de vez, ambos na própria história `feat/liveview-customizavel`, a pedido do navigator testando a página real; é link de verdade desde essa história — antes era um item desabilitado/"em construção" — e aponta pra `/` (T7: `LiveViewPage` virou a página principal do sistema, rota `/live-view` consolidada nela; `end: true` no `NavItemDef` pra não ficar marcado ativo em toda rota, já que qualquer pathname começa com `/`)), seção `Câmeras e Gravações` (chamada `Sistema` até a mesma história `refactor/reorganizar-sidebar-ia`, T2 — nome vago pro conteúdo real da seção, feedback do navigator olhando o rail de verdade; Câmeras — todos; os demais, admin-only, nesta ordem — movidos pra cá de dentro do que antes era `Movimentos`, T1 da mesma história: Gravações `/recordings`; Histórico — link direto pra `/history`, `HistoryLandingPage` escolhe a câmera, pulando o picker sozinha se só existir 1; Relatórios `/reports`, link direto: a própria `ReportsPage` já tem um `<select>` de câmera, então escolher antes de navegar era redundante, sem `:cameraId` na URL, `ReportsPage` seleciona a 1ª câmera assim que a lista carrega), seção `Inteligência` (renomeada de `Movimentos` na história `refactor/reorganizar-sidebar-ia`, agrupando só os recursos de IA/análise depois que Gravações/Histórico/Relatórios saíram pra `Câmeras e Gravações` acima; depois encurtada de "Inteligência Artificial" pra só "Inteligência" na história `refactor/camera-tabs-para-sidebar-ia` — pedido do navigator testando a branch: era o label de seção mais longo do rail e quebrava em 2 linhas na largura fixa do rail expandido (`w-48`); o cabeçalho de seção (`SidebarSection`) também ganhou `truncate`+`title` nessa mesma história, como garantia estrutural independente de métrica de fonte; admin, nesta ordem: Análise de vídeo `/settings/analysis`; Rotular eventos `/settings/analysis#label-events`, âncora dentro da mesma página; Detectores de objetos, que deixou de ser o 1º item da seção; Treinadores `/settings/trainers` (id `sidebar-trainers`, ícone `Gauge`, desde a história `feat/trainer-adapter-pattern`); e — desde a história `refactor/camera-tabs-para-sidebar-ia`, que tirou as abas "Análise"/"Estados" de dentro da página de câmera (câmera-primeiro) e trouxe pra cá (IA-primeiro, escolhendo a câmera na própria página) — Estados `/settings/states` (id `sidebar-states`, ícone `Network`), agora o último item da seção; "Análise por câmera" viveu aqui como item próprio (`/settings/analyses`, id `sidebar-analyses`) até a história `refactor/mover-analise-para-cadastro-camera`, quando voltou a ser câmera-primeiro — não como aba, vira sessão (`CameraAnalysisSection`) dentro do cadastro de câmera, ver abaixo), seção `Administração` (admin: Servidor, Rastrear câmeras — migrou de dentro de `Câmeras e Gravações` pra cá, logo depois de Servidor, mesma história `refactor/camera-tabs-para-sidebar-ia`, pedido do navigator —, Usuários, e — desde a história `feat/extensao-telegram` — Preferências `/settings/preferences/extensions`, id `sidebar-preferences`, ícone `Settings` (gear, já existia em `Icons.tsx` sem uso); aponta direto pra `/settings/preferences/extensions`, que já mostra o conteúdo de todas as extensões ali mesmo — `PreferencesExtensionsPage` (ver abaixo) — sem navegar pra sub-rota nenhuma. Aparência (`/settings/appearance`, id `sidebar-appearance`) viveu aqui como link próprio desde `feat/reorganizar-topbar-sidebar` até a história `refactor/mover-appearance-preferencias`, quando migrou pra dentro de Preferências como 2ª aba (`/settings/preferences/appearance`) — o item solo saiu do rail de vez, `Preferências` já é o único ponto de entrada pras duas. Armazenamento (`/settings/storage`, id `sidebar-storage`) seguiu o mesmo caminho na história `refactor/preferencias-submenu-lateral-storage`: saiu de "Administração" e virou item do submenu de `PreferencesLayout` (`/settings/preferences/storage`, ver abaixo) — `Preferências` continua o único ponto de entrada do rail pra Extensões/Aparência/Armazenamento juntos). A seção `Governança` (Gravações/Estatísticas/Relatórios) foi dissolvida na mesma história `refactor/reorganizar-sidebar-governanca`: Gravações/Relatórios migraram pra dentro do que era `Movimentos` na época (hoje `Câmeras e Gravações` — ver acima, história `refactor/reorganizar-sidebar-ia` moveu de novo) e "Estatísticas" saiu do sidebar — seu conteúdo (KPIs de Gravações/Horas gravadas/Câmeras + lista expansível de câmeras com `MotionScoreChart`) migrou pra dentro da página `ServerSettingsPage` (`pages/settings/ServerSettingsPage.tsx`, `/settings/server`), reaproveitando `useStats()`/`/api/cameras` — fecha o ciclo de uma migração incremental (os cards "Sistema" e "Uso de disco" já tinham migrado de `StatsPage` em histórias anteriores). `StatsPage.tsx` e suas rotas (`/settings/stats`, `/stats`) foram removidas. A antiga seção `Aparência` (`ThemeModeNav` + "Cor de destaque" + "Cor de fundo" desabilitada) e o item solo `Sobre` no fim (sem cabeçalho de grupo) saíram do rail nessa mesma história — o 1º virou o link `sidebar-appearance` acima (Estilo/`ThemeModeNav` foi pra `TopBar`, ver abaixo) e o 2º virou o sub-link `about-application` do dropdown `AppHelpMenu`, também na `TopBar`. `SidebarNavLink` aceita `matchHash?: string` — só necessário quando dois itens da mesma seção compartilham pathname e diferem só pelo hash (Análise de vídeo/Rotular eventos), já que o `isActive` nativo do `NavLink` ignora hash.

**`TopBar`** (`components/TopBar.tsx`) — barra fixa (`sticky top-0`) full-width no topo do app, acima da linha `Sidebar`+conteúdo, altura `h-14` (mesma da antiga linha de logo que vivia dentro do `Sidebar`). Logo "os-camera" (id `logo-app`, link pra `/`, à esquerda) e, à direita, o grupo `AppHelpMenu` → `ThemeModeNav` → `MotionNotificationsBell` → `UserMenu` (id `logged-in-user`) — o meio fica vazio de propósito, reservado pra elementos futuros. `AppHelpMenu`, `ThemeModeNav` (id do gatilho `color-mode`) e `MotionNotificationsBell` (id `motion-notifications`) migraram pra cá vindos do `Sidebar` na história `feat/reorganizar-topbar-sidebar` — respectivamente do item solo "Sobre", e das seções "Aparência"/"Eventos" (ver `Sidebar` acima); todos com `showLabel={false}` (ícone só, igual ao `UserMenu`) e ancorando o próprio flyout **abaixo-à-direita** do gatilho (mesma fórmula do `UserMenu`: `top: rect.bottom + 8`, `right: window.innerWidth - rect.right`) — no rail vertical eles abriam à direita do botão (sempre havia espaço), o que vazaria da viewport aqui, perto do canto superior direito. `AppHelpMenu` (`components/AppHelpMenu.tsx`, id do gatilho `app-help`, ícone `HelpCircle`) reusa o hook `useFlyout` (`sidebarFlyout.ts`, mesmo hook do `MotionNotificationsBell`) e tem um único sub-link no painel: "Sobre" (id `about-application`, → `/settings/about`). `UserMenu` renderiza EM FLUXO aqui (não mais `position: fixed`) — antes vivia no rodapé do rail (flyout saía da viewport em telas curtas) e depois virou fixo solto no canto superior direito (colidia com botões de ação dos `PageHeader`, já que `.page-content` é fluido); agora tem um lugar próprio reservado, sem colidir com nada.

**`Layout`** (`components/Layout.tsx`) — shell de página: `TopBar` em cima (full-width, `sticky top-0`) + uma linha `Sidebar` + conteúdo abaixo dela + `Footer` (rodapé estático, "os-camera · monitoramento residencial"; sem versão/uptime/stats — ver "Largura do conteúdo" abaixo). `Sidebar` fica num wrapper `sticky top-14 h-[calc(100vh-3.5rem)]` — gruda logo abaixo da `TopBar` (3.5rem = `h-14` dela) e cobre o resto da altura da viewport, mesmo padrão de sempre (rail fixo mesmo em página com conteúdo mais alto que a viewport). `TopBar`/`Sidebar` só renderizam quando `hideNav` é falso (ex.: player em tela cheia esconde os dois). Toda página do app usa esse shell.

`PageHeader` — cabeçalho padronizado (título + subtítulo opcional + ações à direita); prop `size` (`page`, default, ou `section` — usada nas sub-páginas de `/settings/cameras/*`, que já têm o breadcrumb do `CameraSettingsTabs` como contexto).

`Player` (`components/Player.tsx` — nome antigo `HLSPlayer`) — player de live enxuto, **WebRTC-first com fallback automático pro HLS**: tenta o transporte WebRTC de baixa latência via `lib/webrtc.negotiateWebRTC` (`POST /api/cameras/{id}/webrtc`) e, quando indisponível (`409`/câmera não-H.264), a negociação falha, ou a conexão não fecha em 5 s (watchdog), cai pro `hls.js`. A prop `transport` (o `live_transport` da câmera: `auto`/`webrtc`/`hls`) pula o WebRTC quando `hls`. Autoplay mudo, overlay de loading/tap-to-play, retry com backoff+teto (`components/playerRetry.ts`), zoom (`hooks/usePlayerZoom.ts`, controle explícito no rodapé via `Zoom`, ver abaixo); rodapé (`PlayerFooter`, ver abaixo) com nome da câmera + botões de mudo/`Zoom`/snapshot/tela cheia (nessa ordem) quando `title`/`controls` são passados. Snapshot (`hooks/usePlayerSnapshot.ts`, compartilhado com `VideoPlayer`, ver abaixo) captura o frame atual do `<video>` num `<canvas>` (`drawImage` nas dimensões nativas) e baixa como PNG (`toBlob`+`URL.createObjectURL`+`<a download>` temporário) — sem chamada ao backend, o frame já está decodificado no elemento; nome do arquivo prefixado com o `title` da câmera.

`PlayerFooter` (`components/PlayerFooter.tsx`) — rodapé compartilhado por todo player, theme-aware (`bg-surface`/`text-foreground`/`border-border` — nunca cor fixa tipo `bg-black`/`text-white`, mesmo sendo "chrome" de vídeo). Dois modos: com `title` (nome da câmera à esquerda + `children` como ações à direita, numa linha só — uso do `Player`, ex. `LiveViewPage`) ou **freeform** (`title` ausente — renderiza só `children` dentro do container, pro chamador montar o próprio layout — uso do `VideoPlayer`, cujo rodapé tem barra de progresso + linha de controles + linha extra opcional).

`VideoPlayer` (`components/VideoPlayer.tsx`) — motor de reprodução de N segmentos MP4 em sequência (double-buffering, 2 `<video>` empilhados, sem tela preta na fronteira — MP4 não-fragmentado, MSE não é opção), compartilhado por `HistoryPage`/`VideoBrowserPage`. Rodapé único (`PlayerFooter` freeform) com barra de progresso arrastável, play/pause, repeat, mudo, dropdown de velocidade, contador de segmento (opcional, `segmentCounter`) e tela cheia — **sempre visível** (não mais um overlay que só aparecia no hover) e theme-aware, mesmo tratamento do rodapé do `Player`. O controle de zoom (`Zoom`, ver abaixo) vive no rodapé (entre mudo e o dropdown de velocidade), condicionado à prop `zoom?: boolean` (default `true`) — substituiu o antigo chip `PlayerControlsOverlay`, que ficava sobreposto ao vídeo e só aparecia quando já estava zoomado (removido, sem consumidor restante). Logo depois do zoom, dois botões sempre presentes (história `feat/liveview-player-footer-limpeza`): snapshot (mesmo `hooks/usePlayerSnapshot.ts` do `Player`, capturando o `<video>` ativo — `getActiveVideoEl`) e download da gravação (`<a download>` cru, `href` = `src` do segmento ATIVO no momento — `segments[curSeg]?.src` —, já carrega o token via querystring como qualquer outro `src` de vídeo; troca de segmento durante a reprodução atualiza o `href` junto). Prop `footerExtra?: ReactNode` — linha extra dentro do rodapé, abaixo dos controles, pra a página injetar conteúdo específico sem o motor genérico precisar conhecê-lo (ex.: `HistoryPage` usa isso pro switch de reprodução contínua + `DatePicker`, ambos antes soltos acima da tira de gravações). Prop `overlay?: ReactNode` — conteúdo sobreposto ao vídeo (loading/erro), aditivo, sempre visível. `seekGlobal` (arraste da barra de progresso própria) é chamado a cada pixel de movimento durante um arraste (dezenas de vezes por segundo) — um seek BACKWARD (arrastar/tempo decrescente) custa mais pro decoder que um forward (precisa voltar pro keyframe anterior e decodificar pra frente, em vez de só continuar decodificando os próximos frames); as gravações são copiadas do stream da câmera sem reencode (`internal/recorder`, `-c copy`), então o intervalo de keyframe é o da própria câmera, fora do nosso controle. Um throttle por TEMPO fixo (ex.: 1x por `requestAnimationFrame`, ~60/s) não é suficiente: um seek backward pra um keyframe distante pode levar bem mais que 16ms pra resolver (mais ainda num Raspberry Pi — hardware suportado, ver `make rpi`), e pedir o próximo antes do anterior terminar força o decoder a abortar/reiniciar repetidamente — vídeo "piscando" ao arrastar pra trás (reportado pelo navigator). Em vez de um intervalo fixo, `seekGlobal` espera o elemento ativo sinalizar `el.seeking === false` (poll via `requestAnimationFrame`, sem novo listener) antes de aplicar o próximo seek pendente — ritmo ditado pelo PRÓPRIO decoder, nunca mais rápido do que ele consegue de fato acompanhar. A posição visual (`pos`, barra/playhead) continua síncrona a cada chamada — só a mutação real do `<video>` espera; a última posição pendente é a que vale (intermediárias descartadas).

`Zoom` (`components/Zoom.tsx`) — controle de zoom explícito e sempre visível (`− 100% +`), compartilhado por `Player` e `VideoPlayer`: recebe `{id, zoom: PlayerZoom}` e só renderiza os 3 botões (zoom out, percentual — clicável, chama `zoom.reset` — e zoom in), sem lógica de zoom própria. `usePlayerZoom.ts` (o hook por trás do scroll-to-zoom/drag-to-pan que os dois players já usavam) ganhou `zoomIn`/`zoomOut` (mesmo fator do scroll — `WHEEL_FACTOR` — só que ancorado no CENTRO do container em vez do cursor, reaproveitando a mesma `zoomAtPoint` de `pages/playerZoom.ts`) e `canZoomIn`/`canZoomOut` (`scale < MAX_SCALE`/`scale > MIN_SCALE`) pra habilitar/desabilitar os botões nas pontas.

`RecordingPlayerModal` (`components/RecordingPlayerModal.tsx`) — reproduz uma gravação/clip em modal, sem sair da página que abriu (uso: `RecordingsPage`). Player "agnóstico": recebe só `open`/`cameraId`/`recordingId`/`motionId?`/`onClose`, nenhum estado da página chamadora — qualquer ponto do sistema pode reaproveitar. Mesmo padrão de modal "de verdade" do `ConfirmDialog` (portal pra `document.body`, `z-10000`, `useEscapeKey`), com a diferença de fechar também no clique do backdrop. Consome `useRecordingSegments` (`hooks/useRecordingSegments.ts`) — hook que resolve `cameraId`+`recordingId(/motionId)` em `VideoPlayerSegment[]`, extraído da lógica que também vive (duplicada, por ora — consolidar é trabalho futuro opcional) dentro do `useEffect` de `VideoBrowserPage.tsx`; `cameraId`/`recordingId` nulos (modal fechado) não disparam fetch. `idPrefix="recording-player"` passado ao `VideoPlayer` interno é deliberadamente distinto do `id="recording-player-modal"` do wrapper do modal — `VideoPlayer` usa `id={idPrefix}` cru no próprio container, então usar o mesmo valor colidiria. **Arrastável e redimensionável** (pedido do navigator): a caixa (`#recording-player-modal-box`) não é mais centralizada por flex no backdrop — sua posição/tamanho vêm de `useDraggableResizable` (`hooks/useDraggableResizable.ts`, Pointer Events puros, sem lib — nenhuma lib de drag/resize existe no projeto, mesma convenção de `usePlayerZoom`/popover do `DatePicker`). A altura NUNCA é estado próprio: é sempre derivada da largura (`width / aspectRatio + chromeHeight`), o que trava a proporção do vídeo (16:9) por construção — impossível os dois divergirem, já que só existe um estado (`width`). O cabeçalho (`#recording-player-modal-header`) é a alça de arrastar (`cursor-move`; ignora cliques que começam num `<button>` — "Visualizar no histórico"/fechar — mesmo guard que `usePlayerZoom` já usa pro pan); uma alça nova no canto inferior direito (`#recording-player-modal-resize-handle`) redimensiona (`cursor-nwse-resize`, visual via `repeating-linear-gradient` CSS — sem ícone novo, deliberado: `Icons.tsx` só tem path data extraído de verdade do lucide-react, inventar um path novo arriscaria não bater com o SVG real). `MODAL_CHROME_HEIGHT` (altura fixa de cabeçalho+padding+rodapé do `VideoPlayer`, somada por cima da área do vídeo) é uma estimativa calculada a partir da estrutura renderizada, não medida via `ResizeObserver` — ao contrário do padrão "medir em vez de chutar" já usado em outros lugares (`HistoryPage`'s `mainHeight`), aqui é a primeira tentativa desse número, não uma correção repetida; vale checagem visual pontual se algum dia o valor se mostrar errado no browser real.

`CameraStageHeader` + `CameraViewTabs` — cabeçalho (nome + badge REC + abas "Ao vivo"/"Histórico") compartilhado por `LivePage`/`HistoryPage`; a aba ativa vira `<span>` (não link, já que clicar de novo não levaria a lugar nenhum) e o dot verde da aba "Ao vivo" só pulsa quando ela está ativa (sinaliza "streamando agora").

`MotionNotificationsBell` — sino de notificações de movimento na `TopBar` (id `motion-notifications`; viveu antes no topo do `Sidebar`, ver acima); abre painel com lista, seleção múltipla, marcar lido/excluir; clique num item resolve e navega pra gravação via `lib/eventNavigation.resolveEventRecordingUrl`.

`ProfileLayout` — layout do Perfil (chegada via `UserMenu`): coluna com 3 links (Perfil / Alterar e-mail / Alterar senha — ids `profile-nav-perfil`/`profile-edit-email`/`profile-edit-senha`) sobre o `Layout`, sem lista de seções administrativas (não é o mesmo sistema do settings sidebar). "Alterar e-mail" (história `feat/profile-rotas-edit-change-email`) substituiu o botão inline que existia no card de visualização de `ProfilePage`; "Alterar senha" foi renomeado de `profile-nav-senha` pra `profile-edit-senha` na mesma história (convenção `edit` pra ações de navegação que levam a um fluxo de edição, não só uma sub-página informativa). A lista de `NavLink`s em si (`<aside><nav><ul>`) é `SectionNavList` (`components/SectionNavList.tsx`) — único consumidor hoje (`SettingsLayout` tinha uma coluna equivalente, removida, ver abaixo). `ProfilePage` é montado em 3 rotas (`/profile`, `/profile/edit`, `/profile/change-email`, declaradas em `App.tsx`) — ver "Padrão: edição via rota dedicada" no início desta seção.

`SettingsLayout` (`components/SettingsLayout.tsx`) — envoltório fino de página pra **todas** as páginas de `/settings/*` (topo e sub-páginas por entidade, ex. `/settings/cameras/<seção>/:id`, `/settings/users/:id`): só `Layout` + `.page-content`, `id`/`footerId` repassados por prop pra cada página manter seu id único — **sem** coluna de navegação própria. Teve uma coluna persistente (`<aside id="settings-nav">`, espelhando `SectionNavList`) que foi removida (história `feat/reorganizar-menu-navegacao`, T6): o rail (`Sidebar`) já mostra a hierarquia completa de seções sempre visível, então uma 2ª coluna idêntica ao lado virou redundância pura (pedido do navigator, ao ver as duas colunas lado a lado).

`CameraSettingsTabs` — só as abas Câmera/Zonas no topo das páginas de configuração por câmera (`/settings/cameras/<seção>/:id`), sem mais nada — Análise e Estados saíram daqui na história `refactor/camera-tabs-para-sidebar-ia` (ver `Sidebar` acima); "Detecção de movimento" saiu na história `feat/camera-form-reshape` (T4) — deixou de ser aba/rota própria (`/settings/cameras/motion/:id`, removida) e virou sessão dentro de `/settings/cameras/:id`, a única aba que resta além dela é `Zonas`. O botão "+ Nova câmera" **não vive mais aqui**: chegou a ficar nesta linha (história `refactor/camera-detail-secoes-aplicar`, T5, com um fix de alinhamento via `items-end`), mas o navigator pediu explicitamente pra ele ocupar a MESMA posição/estilo em toda tela de câmera (visualização, zonas e lista) — migrou pro `PageHeader.actions` de `CameraDetailSettingsPage`/`CameraZonesSettingsPage` (id `camera-create`, mesmo id/estilo do botão já existente em `CamerasSettingsPage`, a lista — nunca co-montados, então o id repetido é seguro), a única posição que já era consistente antes.

`PreferencesLayout` (`components/PreferencesLayout.tsx`) — submenu LATERAL de Preferências, história `refactor/preferencias-submenu-lateral-storage`, substitui de vez o antigo `PreferencesTabs` (abas horizontais, removido). **Só 3 links fixos e estáticos** — Extensões (`/settings/preferences/extensions`), Aparência (`/settings/preferences/appearance`), Armazenamento (`/settings/preferences/storage`) — sem fetch, sem agrupamento, sem sub-navegação por extensão: um desenho anterior (T1/T2 da mesma história) tinha "Extensões" como grupo não-clicável com 1 item de navegação por extensão (cada uma com rota própria) — revertido a pedido do navigator testando a branch ("devem ser os únicos links no submenu"; "quando clicar em Extensões devem ser listadas as extensões, o conteúdo"). Prop `active: 'extensions' | 'appearance' | 'storage'` destaca o item atual via `aria-current="page"`. Renderizado DENTRO do `SettingsLayout`/`PageHeader` de cada página-filha (mesma posição que `PreferencesTabs` ocupava) — não é um wrapper que os substitui; `children` é o conteúdo da coluna direita. Os 3 links reusam `navItemClass` (`components/sidebarFlyout.ts`, mesma classe do `Sidebar`, chamada com `showLabel=true`) dentro de um `<nav className="flex flex-col gap-1">` — história `fix/preferencias-submenu-espacamento`: antes eram uma classe local (`block px-3 py-1.5`) sem `gap` nenhum entre eles, então o hover de um link encostava no vizinho (achado pelo navigator testando a branch); reusar a classe do rail em vez de duplicar valores também unificou o estado ativo pro mesmo fundo sólido (`bg-primary text-on-primary`) usado em toda navegação do app.

`PreferencesExtensionsPage.tsx` (`/settings/preferences/extensions`) mostra o conteúdo de **todas** as extensões juntas, na mesma página — clicar em "Extensões" no submenu não navega pra sub-rota nenhuma. Cada extensão é um componente `<Nome>ExtensionCard` próprio, empilhado: `TelegramExtensionCard.tsx` (nome+descrição, e se `available` um checkbox "Ativado" — estado local, staged — + botão "Aplicar" que persiste via `PUT /api/settings/extensions/telegram`; se `!available`, só a mensagem "Extensão não permitida nesta instância.", sem nenhum controle) e `S3ExtensionCard.tsx` — **um card só** (desenho anterior tinha 2 cards separados, Ativado + formulário de destino — revertido a pedido do navigator vendo a branch real: "isso eu entendo que deva ser apenas um card: se for clicado em ativar, abrir as opções para configurar"): checkbox "Ativado" no topo: quando marcado, revela o formulário de destino S3 (nome/bucket/credenciais) logo abaixo, dentro do MESMO card; um único botão "Aplicar" salva os dois juntos — se `Ativado` estiver marcado, primeiro grava o destino (`POST`/`PUT /api/retention-extensions`, singleton — S3 é sempre 0 ou 1 linha), depois liga o toggle (`PUT /api/settings/extensions/s3`); desmarcar e aplicar só desliga o toggle, sem apagar a config salva (fica pronta pra reativar sem preencher tudo de novo) — apagar de vez é "Excluir configuração" (`s3-config-delete`), ação separada com `ConfirmDialog`. Nenhuma das duas páginas por extensão de antes (`TelegramExtensionPage.tsx`, `S3ExtensionConfigPage.tsx`) nem a landing que resolvia qual abrir primeiro (`PreferencesExtensionsLandingPage.tsx`) existem mais.

`CameraForm` (`components/CameraForm.tsx`) — formulário de câmera, usado hoje **só** em `/settings/cameras/new` via `CamerasSettingsPage` (criação — precisa continuar sendo 1 form único com 1 Salvar/Cancelar: não existe câmera existente pra aplicar patches parciais seção por seção numa criação). Deixou de ser usado em `/settings/cameras/edit/:id` na história `refactor/camera-detail-secoes-aplicar` (rota removida — ver `CameraDetailSettingsPage`/`CameraCaptureSection` abaixo), mas os blocos de campos que ele monta continuam reusados pelas novas seções de edição (ver T1 abaixo). Reorganizado em sessões com responsabilidade clara (história `feat/camera-form-reshape`) — sem bucket genérico "Configurações avançadas" (removido por completo, T3): **Nome** (sozinha, sem painel); **Captura** (painel, propriedades da CONEXÃO em si, não do que se faz com ela — `capture_type` "Protocolo" antes da URL, rótulo/placeholder condicionais RTSP/HLS, `video_codec`/`has_audio`/`resolution` — metadados de stream detectados, consumidos por recorder E streamer HLS E motion, não exclusivos de Gravação —, `reconnect_interval` — usado por recorder/streamer/webrtc igualmente); **Gravação** (painel, `recording_enabled` esconde de verdade `chunk_duration`/`record_video_mode` quando desligado); **Transmissão** (painel, `live_enabled` — campo novo e separado de `live_transport`, não um 4º valor do enum — esconde `live_transport`+campos HLS quando desligado). Painéis (exceto Nome) seguem o mesmo estilo visual de `SettingsSection` (`bg-surface border border-border rounded-lg` + título `text-h4 uppercase tracking-wider`, uma classe reaproveitada diretamente, não uma nova). `capture_type`/`live_enabled` já existem e funcionam no backend desde as histórias `feat/hls-capture-fundacao`/`feat/hls-capture-backend-completo` — este form só os expõe. **`live_transport=webrtc` força `video_codec='h264'`** (select desabilitado enquanto webrtc está selecionado, pedido do navigator testando a branch): WebRTC só toca H.264 no browser, e o backend (`webrtc.ShouldRunHLS`) só desliga o pipeline HLS quando o codec RESOLVIDO é H.264 — sem forçar o campo, a UI não tinha como garantir que o HLS de fato para de rodar (câmera configurada como "Auto" podia continuar publicando HLS por trás, contradizendo os campos escondidos). Com o codec forçado, `ShouldRunHLS` fica determinístico e os campos exclusivos de HLS (`hls_video_mode`+segmento+janela+DVR) somem de vez (não só uma nota explicativa) quando `live_transport==='webrtc'` — nota `text-xs` avisa "HLS desligado" no lugar. Se o "Detectar" (`handleDetectStreams`) mostrar que o stream real não é H.264 com WebRTC selecionado, o aviso deixa claro que não há mais fallback automático pro HLS (como havia antes de forçar o codec) — o usuário precisa trocar pra "Automático" ou "HLS" manualmente. O `video_codec` anterior (ex.: um `hevc` explícito) é guardado num `useRef` antes de forçar e **restaurado** ao sair do WebRTC — achado da 2ª iteração de review: sem isso, um toggle exploratório pra WebRTC e de volta perdia o valor customizado de vez, sem aviso. Os blocos de campos (`NameField`/`CaptureFields`/`RecordingFields`/`TransmissionFields`) foram extraídos pra `components/cameraFormFields.tsx` na história `refactor/camera-detail-secoes-aplicar` (T1) — presentacionais puros, sem estado próprio (`form`/`set` por prop), reusados pelas novas seções sempre-editáveis da edição de câmera (`CameraCaptureSection`/`CameraRecordingSection`/`CameraTransmissionSection`, ver abaixo). `CaptureFields` recebe `codecDisabled` por prop (em vez de olhar `live_transport` do próprio form) e `TransmissionFields` recebe `rtspUrl` por prop (em vez de ler `form.rtsp_url` direto) — desacopla a JSX de ONDE esses valores vêm, permitindo que o form único de criação e as seções independentes de edição usem fontes diferentes sem duplicar markup.

`CameraMotionSection` (`components/CameraMotionSection.tsx`, exporta `MotionFormContent`/`MotionReadOnly`) — sessão "Detecção de movimento", migrada de `CameraMotionSettingsPage.tsx` (removida) pra dentro de `CameraDetailSettingsPage.tsx` na história `feat/camera-form-reshape` (T4). Mantém `<form>`/botão `motion-save` **independente** do `camera-form-save` de `CameraForm` — decisão deliberada (menor risco, sem unificar os dois submits); renderiza sempre, tanto em modo visualização quanto edição da câmera (não é gated pelo `editing` derivado da rota — só o próprio `motion_enabled` controla o que aparece dentro dela). O campo de substream RTSP (`camera-motion-rtsp-url`/`camera-motion-rtsp-detect`, condicionado a `capture_type==='rtsp'`) mora aqui — migrou de dentro de `CameraForm` (onde tinha pousado temporariamente no T3) pro seu dono conceitual de verdade nesta história. Título/borda do painel ("Detecção de movimento") vivem num wrapper privado `MotionPanel` DENTRO deste módulo — usado por `MotionFormContent`/`MotionReadOnly` internamente, nunca pelo chamador (`CameraDetailSettingsPage` invoca os dois componentes direto, sem embrulhar de novo), pra nunca duplicar título/borda. Sub-blocos internos ("Configuração", "Hoje") usam `FieldGroup` (privado, label + grid `label/valor` sem `Card`/borda própria) em vez de `SettingsSection` — `SettingsSection` aninhado aqui criava painel-dentro-de-painel (feedback do navigator vendo a página real: "tem outro painel chamado Hoje, organize melhor"). `handleSave` de `MotionFormContent` NUNCA envia o objeto `form` inteiro pro PUT — `MotionFormContent` fica montado o tempo todo (não remonta ao entrar/sair de edição da câmera, ao contrário de `CameraForm`), então seu `form` é um snapshot de `cam` que pode estar desatualizado se outra sessão (Nome/Captura/Gravação/Transmissão) salvou nesse meio-tempo; o payload sempre parte de `{ ...emptyForm(cam), <só os campos de movimento/substream> }` — dados atuais de `cam` por baixo, só os campos que esta sessão de fato edita por cima — pra salvar aqui nunca reverter silenciosamente uma edição feita em outra sessão.

`CameraCaptureSection` (`components/CameraCaptureSection.tsx`) — sessão "Nome e Captura" de `CameraDetailSettingsPage`, história `refactor/camera-detail-secoes-aplicar` (T2): generaliza pra Nome+Captura o mesmo padrão que `MotionFormContent`/`CameraAnalysisSection` já usavam (sempre editável, `<form>`/Aplicar próprio, `useState(() => emptyForm(cam))` sem resincronizar, payload parcial sobre `emptyForm(cam)` fresco) — antes, Nome/Captura viviam dentro do form único `CameraForm`, atrás de um botão "Editar" que alternava a página inteira entre visualização/edição (removido, ver `CameraDetailSettingsPage` abaixo). Monta `NameField`+`CaptureFields` (extraídos em T1, ver `CameraForm` acima) dentro do MESMO painel visual — título "Nome e Captura" — e do mesmo `<form>`/Aplicar (Nome não ganhou seção própria — decisão do navigator: campo único, painel dedicado seria desproporcional). Achado do navigator em pré-push: a 1ª versão só compartilhava o `<form>` (mesmo Aplicar), mas `NameField` ficava fora da borda do painel — visualmente dois blocos separados, contradizendo "Nome continua vivendo dentro do mesmo bloco de Captura"; corrigido movendo `NameField` pra dentro da mesma `<div className="bg-surface border...">`. `codecDisabled = (cam.live_transport ?? 'auto') === 'webrtc'` é derivado do `cam` PERSISTIDO (prop), não de um form de outra seção em edição — é `CameraTransmissionSection` (ver abaixo) que garante a invariante "webrtc ⇒ h264" no PRÓPRIO save dela; esta seção só reflete o estado já salvo (e, quando `codecDisabled`, também FORÇA `video_codec:'h264'` no seu próprio payload, garantindo que o campo nunca saia dessincronizado mesmo se o form local tiver um valor customizado não-aplicado).

`CameraRecordingSection`/`CameraTransmissionSection` (`components/CameraRecordingSection.tsx`/`CameraTransmissionSection.tsx`) — sessões "Gravação"/"Transmissão", mesma história (T3), mesmo padrão de `CameraCaptureSection`/`MotionFormContent` (sempre editáveis, Aplicar próprio, payload parcial sobre `emptyForm(cam)` fresco). `CameraRecordingSection` monta `RecordingFields` sem nada de especial. `CameraTransmissionSection` monta `TransmissionFields` com `rtspUrl={cam.rtsp_url}` (fresco, prop — não um form local, já que o campo RTSP agora mora numa seção DIFERENTE, Captura; mesmo motivo de `MotionFormContent.handleDetectSubstream` já usar `cam.rtsp_url`) e é **esta seção que garante a invariante "webrtc ⇒ `video_codec=h264`"**: ao aplicar com `live_transport==='webrtc'`, o payload força `video_codec:'h264'` no PRÓPRIO save, independente do form local (não-salvo) de `CameraCaptureSection` no momento. Ao SAIR do webrtc, não mexe em `video_codec` — a restauração automática do codec customizado anterior (que existia no antigo `CameraForm` único, via `useRef`) foi **descontinuada**: com os saves independentes, esta seção não tem mais acesso ao form local de Captura pra restaurar algo; o usuário escolhe o codec de novo lá, se quiser, depois de sair do WebRTC.

`CameraAnalysisSection` (`components/CameraAnalysisSection.tsx`) — sessão "Detector de objetos" (título exibido — pedido do navigator, já que o campo central do card é justamente qual detector a câmera usa; o nome do componente/arquivo continua `Analysis`, ligado à API por trás), migrada de `CameraAnalysisSettingsPage.tsx` (removida, rota `/settings/analyses/:id`) pra dentro de `CameraDetailSettingsPage.tsx` na história `refactor/mover-analise-para-cadastro-camera` — mesmo precedente já resolvido por `CameraMotionSection` (acima). Renderizada **só no branch admin**, logo depois de `MotionFormContent` e antes de `DeviceInfoPanel` — diferente de Motion, não tem equivalente no branch viewer: a API (`GET/PUT /api/settings/cameras/{id}/analysis`) é `authAdmin`, um viewer não teria dado nenhum pra mostrar. Recebe só `id` como prop (não o objeto `cam` inteiro — as chamadas são só por id, sem campos da câmera envolvidos). Checkbox Habilitado + select Detector (lista vinda de `GET /api/settings/detectors`) + slider de Limiar de confiança (escondidos quando desabilitado) + botão "Aplicar" próprio (`camera-analysis-save`), mesmo padrão de submit independente que `CameraMotionSection` já usa pra `motion-save`. Rótulo, alinhamento e TAMANHO unificados na história `refactor/camera-detail-secoes-aplicar` (T5 + fix de pré-push): era "Salvar"/`justify-between` (botão à direita, destoando das demais seções) e sem `size="sm"` (ficava maior — `h-9` — que o Aplicar das outras 4 seções, `h-8`) — virou "Aplicar" (mesmo texto do navigator, "colocar um botão aplica") + `flex items-center gap-3` (à esquerda) + `size="sm"`, igual a `CameraMotionSection`/`CameraCaptureSection`/`CameraRecordingSection`/`CameraTransmissionSection` — achado do navigator só depois de ver a página real, testando lado a lado com "Detecção de movimento" ("não segue o mesmo padrão dos outros").

`CameraDetailSettingsPage.tsx` — branch **admin** (papel com permissão de editar) não alterna mais entre visualização/edição desde a história `refactor/camera-detail-secoes-aplicar`: sem botão "Editar", sem rota `/settings/cameras/edit/:id` (removida de `routes.tsx`; o link "Editar" da lista em `CamerasSettingsPage.tsx` aponta direto pra `/settings/cameras/:id`), sem `EntitySubtitle` (subtítulo é sempre só o nome da câmera). Monta `CameraCaptureSection`+`CameraRecordingSection`+`CameraTransmissionSection` (sempre editáveis, ver acima) seguidas de `MotionFormContent`+`CameraAnalysisSection` (que já eram sempre-editáveis desde antes) — as 5 seções da página seguem agora o MESMO padrão. Motivo da mudança: o navigator reportou (com screenshot) que alternar pra "Editar" abria todas as seções de uma vez sem deixar claro se o "Salvar" de baixo salvava só o que estava acima ou a página inteira, e sugeriu "um botão Aplica" por seção — confirmado via pergunta direta que a página deveria abandonar de vez o modo Editar, não só dividir o Salvar mantendo o toggle. `SettingsSection` "Identificação" agora mostra só o ID (Nome migrou pra dentro de `CameraCaptureSection`, editável ali). `PageHeader.actions` ganhou o botão "+ Nova câmera" (id `camera-create`, só admin) — migrado de dentro de `CameraSettingsTabs` (ver acima) nesta mesma história, pedido do navigator pra ocupar a MESMA posição/estilo do botão equivalente na lista (`CamerasSettingsPage`); `CameraZonesSettingsPage.tsx` ganhou o mesmo botão no mesmo lugar, pelo mesmo motivo — as 3 telas de câmera (lista/detalhe/zonas) mostram "+ Nova câmera" sempre no PageHeader, nunca mais na linha das abas.

Branch **viewer** (sem permissão de editar) **não mudou**: continua usando `CameraCaptureView` (privado ao arquivo) — espelha as MESMAS sessões (Captura/Gravação/Transmissão, "fechadas": `SettingsSection` read-only em vez de inputs), com os MESMOS condicionais de show/hide (`recording_enabled`, `live_enabled`, e os campos HLS somem quando `live_transport==='webrtc'`, mostrando só "HLS: Desligado"). `CameraCaptureView` recebe um shape estrutural (`CameraViewFields`) satisfeito tanto por `Camera` (admin, `cameraFormUtils`) quanto por `CameraSettings` (viewer, `useSettings`) — mesmo componente atendia os dois papéis até esta história (agora só o viewer o usa; o admin usa as seções sempre-editáveis). Pro papel viewer, `cam` vem de `GET /api/cameras` (`handleCameras`, `authFull` — não `authAdmin`, diferente de `GET /api/settings`), cujo DTO (`cameraInfo`) tem os campos que alimentam `CameraCaptureView` (`rtsp_url` mascarado via `maskRTSP`, `capture_type`, `chunk_duration`, `reconnect_interval`, `hls_video_mode`, `record_video_mode`, `live_enabled`, `hls_segment_seconds`, `hls_list_size`, `hls_dvr_seconds`). `DeviceInfoPanel` renderiza sempre por último, depois de `MotionFormContent`/`MotionReadOnly` (viewer) ou `CameraAnalysisSection` (admin).

`SettingsSection` (card com lista de campos label/valor), `ConfirmDialog` (modal fixo de confirmação para ações destrutivas; prop `danger` alterna botão vermelho/azul), `MotionScoreChart` (gráfico SVG em tempo real dos scores brutos via SSE, escala logarítmica, janela de 30 s).

**Páginas principais** (todas usam `Layout`):
- `LivePage` (`/live/:cameraId`) — `CameraStageHeader` + `Player`.
- `HistoryPage` (`/history/:cameraId(/:recordingId)`) — gravações do dia + player + tira de cards pra trocar de gravação; `:recordingId` na URL pré-seleciona (resolvida via `RecordingsGateway.getRecording`) e a seleção mantém a URL sincronizada — link compartilhável. Reprodução contínua (`toggleContinuous`) e o calendário (`DatePicker`) vivem no rodapé do player (`VideoPlayer`'s `footerExtra`, ver acima) — não mais soltos acima da tira de gravações; o cabeçalho "Gravações · N" continua ali, como rótulo da tira.
- `VideoBrowserPage` (`/recording/:cameraId/:recordingId(/:motionId)`) — reprodução via `RecordingsGateway` (único intermediário com o backend); transição entre chunks com double-buffering (2 `<video>` empilhados, sem tela preta na fronteira — MP4 não-fragmentado, MSE não é opção). `:motionId` monta o clip com lead/trail em torno do evento.
- `RecordingsPage` (`/recordings(/:date(/:hour(/:view)))`) — gravações + "momentos" (eventos de movimento/pessoa/ia/estados) agregados multi-câmera; filtro de categoria (multi-seleção, `Set<string>`) + janela de horas. Clique numa gravação (aba Gravações) ou num momento (aba Momentos) abre o player em `RecordingPlayerModal` (ver abaixo) — não navega mais pra `/recording/:cameraId/:recordingId(/:motionId)`, mas a rota continua existindo intacta pra outros pontos de entrada (notificações, deep-link, `MotionsPage`).
- `ReportsPage` (`/reports/:cameraId/:date/:days`) — histograma empilhado de eventos por categoria, por câmera, num intervalo de dias.
- `LiveViewPage` (`/`, página principal do sistema desde a história `feat/liveview-customizavel` T7 — antes a landing era a `AllCamerasPage`, removida; o onboarding que vivia lá — sem nenhuma câmera cadastrada e usuário admin → redireciona pro cadastro, `/settings/cameras/new` — migrou pro `useEffect` de fetch de câmeras aqui) — grid customizável de todas as câmeras ao vivo, via `react-grid-layout/legacy` (subpath com API v1 clássica — `WidthProvider`/`default` — a lib teve um rewrite pra v2 baseado em hooks, mas o subpath `/legacy` mantém 100% de compatibilidade runtime, escolhido deliberadamente pra reduzir risco). O arranjo automático, a persistência em `localStorage` (chave `liveview-layout`) e a reconciliação do layout salvo com a lista atual de câmeras (câmera removida some, nova entra no fim) vivem em `lib/liveViewLayout.ts` — módulo puro, sem depender de `react-grid-layout` nem de medição real de DOM (indisponível no jsdom dos testes). Sem câmeras cadastradas, mostra mensagem vazia em vez do grid. **Presets de layout** (1×1/2×2/3×3/4×4) — clicar reseta TODAS as câmeras VISÍVEIS pra 1 célula cada (`presetLayout(cameraIds, cols)`, wrap na ordem esquerda→direita/cima→baixo) e persiste tanto o preset (`cols`, chave `liveview-cols` via `loadSavedCols`/`saveCols`) quanto o layout resultante; `defaultLayout` delega pra `presetLayout(ids, DEFAULT_COLS)` (`DEFAULT_COLS=3`, mesmo arranjo de sempre quando não há preset salvo). Os 4 presets vivem num **dropdown** (gatilho `live-view-preset-trigger`, mostrando o preset atual, ex. "3×3", + ícone `LayoutGrid`; flyout `live-view-preset-menu` com as 4 opções, `Check` na ativa — pedido do navigator: "coloque isso num dropdown parecido com o color-mode") — mesmo padrão de `ThemeModeNav`/"color-mode": reusa `useFlyout` (`sidebarFlyout.ts`), portal pro body, ancorado abaixo-à-direita do gatilho; **desestruture o retorno de `useFlyout` em variáveis próprias** (não mantenha como um objeto único acessado via `objeto.pos.top` etc.) — o eslint (`react-hooks/refs`) não consegue distinguir essa leitura de state de um acesso a `objeto.btnRef.current` (ref) quando ambos vêm do mesmo objeto, e barra o build. Uma "grade customizada" (botão "Mais" + input, aceitando N solto ou depois "LxC" pra grade assimétrica) chegou a existir nos tickets T5/T6, mas foi revertida a pedido do navigator ("remova essa opção") — só os 4 presets fixos quadrados permanecem. **Grade NxN dimensionada pela viewport** (feedback do navigator testando a página real: um preset "NxN" é uma grade de verdade — N linhas × N colunas — e precisa caber na tela sem exigir scroll pra ver todas as linhas; a 1ª versão calculava `rowHeight` só a partir da LARGURA da coluna, proporcional 16:9, ignorando a altura disponível, o que gerava células grandes demais e forçava scroll num preset com várias linhas): `computeRowHeight(viewportHeight, gridTop, rows, bottomMargin=16, minRowHeight=80)` (`lib/liveViewLayout.ts`, pura) divide a altura disponível (viewport − topo do grid − margem) pelas linhas (sempre = `cols`, grade quadrada) — um preset 1×1 usa a altura toda numa única célula, 4×4 divide essa mesma altura em 4 linhas. A página mede o TOPO do grid via `getBoundingClientRect().top` (ref callback `useCallback`; não depende do próprio `rowHeight`, só do que vem acima — `PageHeader` — então sem realimentação) + `window.innerHeight`, recalculando no evento `resize` da janela. `ResponsiveGridLayout` recebe `containerPadding={[0, 0]}` — a lib aplica 10px de padding interno por padrão, que somado ao `p-6` (24px) da página deixava os tiles ~10px mais afastados da borda que qualquer outra página (bug real reportado pelo navigator: "o conteúdo não segue a margem das outras páginas" — confirmado por medição real via Playwright/`getBoundingClientRect` numa instância isolada, comparando contra `AllCamerasPage`: 1º tile em x=90 vs. x=80 esperado); zerado aqui, os tiles alinham exatamente com a borda do `p-6`, igual a qualquer outra página — o espaçamento ENTRE tiles continua vindo do `margin` padrão da lib (não mexido). `Player` de cada tile tem `controls` (mudo/tela cheia) + badge "GRAVANDO"/"AO VIVO" — paridade com `AllCamerasPage`; o texto do badge é condicional (história `feat/liveview-player-footer-limpeza`): "GRAVANDO" quando `recording_enabled !== false` (ligado por padrão, só vira "AO VIVO" se explicitamente `false` — mesmo critério do badge REC de `CameraStageHeader`). Todos os botões da toolbar usam o componente compartilhado `Button` (`components/ui/button.tsx`, shadcn/`cva` — convenção já estabelecida na maior parte do app), nunca `<button>` cru (exceção: itens de dentro de flyouts/menus — dropdown de presets, menu de "Inserir câmera" — seguem o padrão de item de flyout já usado por `ThemeModeNav`/`AppHelpMenu`, não o `Button`). Redimensionamento manual (arrastar a borda) continua livre e não é travado pra múltiplos NxN — os presets cobrem só o caso comum (arranjo simétrico). **Curadoria de câmeras nesta tela** (feedback do navigator, esclarecido explicitamente: a câmera continua funcionando normalmente no sistema — só sai/entra DESTA tela): toggle "Editar grid" (`live-view-edit-toggle`) trava/destrava `isDraggable`/`isResizable` do grid — o próprio texto/ícone do botão muda quando ativo (`Pencil`+"Editar grid" → `Check`+"Aplicar alterações"), deixando explícito que clicar de novo aplica/conclui as mudanças — e só em modo de edição os tiles mostram um botão de remover no rodapé (`Player`'s `footerTrailing`, mesmo estilo circular dos botões de mudo/tela cheia do próprio `Player.tsx` — não o componente `Button`, que ficaria destoante ao lado deles) — clicar abre `ConfirmDialog` (padrão já usado no app pra ações destrutivas) e, ao confirmar, `removeCameraFromLayout` tira a entrada do layout e o id vai pro `liveview-hidden` (localStorage, `loadHiddenCameraIds`/`saveHiddenCameraIds`) — sem placeholder/quadro vazio no lugar (pedido explícito do navigator, rejeitando o padrão de outro NVR usado como referência). Botão "Inserir câmera" (`live-view-insert-camera`, desabilitado sem candidatos) abre um pequeno menu (mesmo estilo de item de flyout do `AppHelpMenu`) com as câmeras fora da grade agora (novas ou removidas); escolher uma chama `addCameraToLayout` e tira o id de `liveview-hidden`. `mergeLayoutWithCameras` recebe `hiddenIds` como 3º parâmetro (opcional, compatível) — sem isso, recarregar a página traria de volta sozinha uma câmera que o usuário tinha acabado de remover (pra reconciliação, uma câmera oculta pareceria só "uma câmera nova"). Renderiza só `visibleCameras` (interseção de `cameras` com o `layout` atual) — a mensagem vazia distingue "nenhuma câmera cadastrada no sistema" de "nenhuma câmera nesta tela" (todas removidas). O gesto de "arrastar o tile pra cima pra remover" (sugerido pelo navigator, mas com ressalva de ser só uma ideia) foi deliberadamente descartado em favor do botão + `ConfirmDialog` — interação nova sem precedente no codebase, risco desproporcional ao ganho frente a uma necessidade já resolvida.

`NotificationContext` — contexto global que abre um `EventSource` por câmera (via `GET /api/cameras/{id}/motion/live`) e acumula notificações de movimento em `localStorage` (máx. 100). Expõe: `notifications`, `unreadCount`, `markRead`, `markAllRead`, `markSelectedRead`, `markAllUnread`, `remove`, `removeAll`, `removeSelected`. Renderizado pelo `MotionNotificationsBell` (sino com badge no `Sidebar`, painel com seleção múltipla, kebab de ações, `ConfirmDialog` para confirmações).

**Largura do conteúdo nas páginas (`Layout`):** classe compartilhada `.page-content` (`styles/base.css`, hoje só `width: 100%` — **fluida, sem `max-width`**) — fonte única da largura padrão, pra garantir a MESMA largura em toda página (evita divergência: cada página tinha seu próprio `max-w-*` solto e acabaram desalinhando). `<Layout contentClassName="p-6">` (margem de página) + `<div className="page-content space-y-4">` como wrapper do conteúdo — toda página que usa `Layout` segue esse padrão, em vez de repetir `max-w-*` na mão. Já teve um cap (`max-w-[80rem]`, mesmo valor de `max-w-7xl`) pensado pra páginas de **player único** (evitar um vídeo `aspect-video` gigante em telas largas — duas tentativas de largura 100% incondicional foram revertidas antes por esse motivo, ver histórico em commits anteriores desta seção). O cap foi removido na história `feat/reorganizar-menu-navegacao` (T6) a pedido explícito do navigator ("ajuste o layout para todas as páginas tenham a largura da página principal" / "o conteúdo seja fluido") — decisão nova e deliberada que substitui a rationale antiga. `HistoryPage` (`/history/:cameraId`) era a ÚLTIMA exceção a esse padrão (não usava `.page-content` — a página tem uma lista lateral de gravações grande o bastante pra merecer uma 2ª coluna, e o player (`history-main`) e o título levavam um cap de duas colunas só por causa disso); fechada na história `feat/historico-navegacao-largura` (T2), a pedido explícito do navigator, reforçado duas vezes na mesma demanda ("o conteúdo da página deve seguir o mesmo padrão de largura das outras páginas"). `#history-content` é `page-content flex w-full flex-col gap-3` (só o banner de erro solto ali) — **sem** `flex-1`/`min-h-0`: Histórico é página com scroll normal, não uma grade fill-viewport; forçar altura ali faz o `VideoPlayer` (que usa `h-full` internamente) estufar pra ocupar a página inteira — bug já visto, não repetir. O layout de duas colunas em si continua (player à esquerda, lista de gravações à direita, `lg:w-80 lg:shrink-0`) — só o CAP sumiu: `#history-main` é `flex-1` sem `max-w-*`, cresce até preencher o espaço restante de `.page-content`, igual ao player de qualquer página de player único (Ao vivo/Reprodução). O TÍTULO (`#history-header`, dentro do `CameraStageHeader`) fica FORA da linha de duas colunas — só o `children` do `CameraStageHeader` entra nela (`flex flex-col gap-3 lg:flex-row lg:items-start` — sem mais `lg:justify-center`: a linha já ocupa a largura cheia de `.page-content`, não sobra espaço pra centralizar) — assim o topo do sidebar alinha com o topo do PLAYER, não com o topo do título. `CameraStageHeader` (`components/CameraStageHeader.tsx`) não tem mais a prop `twoColumnCap` (removida junto — sem cap nenhum em lugar nenhum, o título sempre acompanha a largura cheia, igual em toda página que o usa, `LivePage` ou `HistoryPage`).

`#history-recordings-list` (sidebar, `lg:w-80 lg:shrink-0`) precisa ter a MESMA altura do fundo de `history-main` (pedido do navigator) — **não** dá pra resolver só com `align-items: stretch` (o default do flex row): sem uma altura EXTERNA de referência (nem a linha nem `history-content` têm altura própria, ambas são "auto" de baixo pra cima), o algoritmo de flexbox usa o MAIOR conteúdo hipotético entre os dois irmãos pra decidir a altura da linha — com uma lista longa de gravações o sidebar vira o maior, e stretch infla `history-main` (e o `h-full` interno do `VideoPlayer` junto) até o tamanho do sidebar, abrindo um vão vazio abaixo do rodapé do player (bug real, visto no navegador — não é hipotético). A solução usada foi medir `history-main` com `ResizeObserver` e aplicar o valor como `maxHeight` inline no sidebar (`overflow-hidden` na caixa externa) — isso quebra o ciclo: a linha leva `lg:items-start` (sem stretch), então `history-main` nunca mais depende do sidebar, só o sidebar depende de `history-main`. O ref é um **ref CALLBACK** (`mainRef` em `HistoryPage.tsx`), não `useRef` + `useEffect(fn, [])`: `history-main` só monta depois que `camera` carrega (fetch assíncrono), então um `useEffect` de deps vazias roda ANTES do node existir (`ref.current` ainda `null` no 1º render) e nunca mais tenta de novo — bug real, confirmado num browser de verdade (Playwright/Chromium local): `mainHeight` ficava `null` pra sempre, sidebar sem teto nenhum, exatamente o vão vazio reportado. Um ref callback dispara toda vez que o node MUDA (attach/detach), inclusive quando ele passa a existir num render posterior — corrige isso de raiz. `ResizeObserver` não existe no jsdom dos testes — degrada graciosamente pra sem teto (guard `typeof ResizeObserver === 'undefined'`). Dentro do sidebar: calendário (`DatePicker`, movido de dentro do rodapé do player — precisa continuar acessível MESMO num dia sem gravação nenhuma, pra dar pro usuário trocar de dia) sempre visível no topo; chips "Tudo/Movimento/Contínua" (`#history-filter-chips`) + `#history-recordings-groups` (grupos por hora, `min-h-0 flex-1 overflow-y-auto scrollbar-thin` — o `min-h-0` é o que permite esse miolo encolher e rolar dentro do teto do pai em vez de ser barrado pelo tamanho mínimo automático do flexbox; `.scrollbar-thin`, classe utilitária em `styles/base.css`, força uma barra de rolagem fina e sempre visível — o overlay auto-hide padrão do browser esconde a única pista de que a lista tem mais conteúdo abaixo) só aparecem quando há alguma gravação no dia (`recordingItems.length > 0`, não o resultado já filtrado `groupsByHour` — senão os chips somem justamente quando um filtro zera a lista, impedindo voltar pra "Tudo"). Sem `footerTrailing` no `VideoPlayer` (o calendário saiu de lá), o `ml-auto` que empurrava `footerEnd` pra ponta direita da linha do rodapé precisou ser reaplicado direto no wrapper do `footerEnd` (`CameraViewTabs`) em `HistoryPage.tsx`. Abaixo do breakpoint `lg` as duas colunas empilham (padrão mobile-first do Tailwind). Diferente do cap fixo de página única, aqui as DUAS colunas somadas usam quase toda a largura da viewport — pedido explícito do navigator ao ver o vão vazio ao lado do player em telas largas.

`ChangePasswordPage` — **single-purpose**: só o fluxo forçado de 1º login (`mustChangePassword()===true`), form solto sem `Layout`/sidebar. Fora desse caso (`/change-password` visitada já logado) redireciona pra `/`. Trocar a própria senha logado é `ProfileChangePasswordPage` (self-service, `/profile/change-password`); trocar a senha de outro usuário é o campo "Senha" (opcional — vazio mantém a atual) do `UserForm`, na edição em `/settings/users/edit/:id`.

`CamerasSettingsPage` (`/settings/cameras`) — lista câmeras como `Card`s (`components/ui/card.tsx`), com thumbnail 80×48 px do snapshot (`GET /api/cameras/{id}/snapshot`). Nome da câmera em `font-medium` (sem `font-mono`), sem badge de ID. Badges via `Badge` (`components/ui/badge.tsx`), cor por status (todos condicionais, só aparecem quando ativos): "Detecção" (verde, `motion.enabled`), "Gravando" (vermelho, `recording_enabled` — mesma cor do badge REC/`--color-recording`), "Análise de objetos" (azul, `analysis_enabled` — campo per-câmera de `camera_analysis_config`, exposto pelos 2 endpoints de listagem de câmera desde a história `feat/badge-cards-responsivo`). Ações Editar/Excluir (só admin) são `Button` com texto sempre visível (não mais ícones que só apareciam no hover). Cada card do grupo de câmeras dentro de `.flex.flex-wrap` quebra em linhas independentes no mobile (nome / badges / ações) sem breakpoint dedicado. No modo admin, suporta drag-and-drop para reordenar: handle de arrastar (⠿) à esquerda de cada linha, destaque azul no alvo, atualização optimista do estado local e chamada a `PUT /api/settings/cameras/reorder` ao soltar.

`UsersSettingsPage` (`/settings/users`) — lista usuários com cada linha em **grid de colunas fixas** (`grid-cols-1 sm:grid-cols-[10rem_10rem_5rem_1fr_auto]`: username/nome/role/câmeras/ações), história `fix/users-cameras-nome-e-tabela` — antes era `flex flex-wrap` sem larguras fixas, então username/nome/badge não alinhavam verticalmente entre usuários com strings de tamanhos diferentes (reportado pelo navigator com screenshot). **As 5 células são sempre renderizadas**, mesmo vazias (`'—'` no nome, mesmo fallback de `ProfilePage.tsx`/`AboutPage.tsx`) — condicionar a presença de uma célula (como antes, `user.role === 'viewer' && (...)`) faria os itens de grid seguintes ocuparem a coluna errada em linhas com menos elementos, quebrando o próprio alinhamento; célula "câmeras" mostra `'todas'` pra admin (mesmo texto de `UserDetailSettingsPage`), `'sem câmeras'`/nomes pra viewer. Sem cabeçalho de coluna (decisão do navigator). Cada célula de texto usa `truncate`+`title` (mesmo padrão de `SidebarSection`) pra não estourar a largura fixa em nomes longos. **A lista/o detalhe de câmeras de um usuário (`user.cameras`, `string[]` de IDs vindo do backend) sempre resolve pra NOME via a lista já buscada em `GET /api/cameras`** (`user.cameras.map(id => cameras.find(c => c.id === id)?.name || id)`, mesmo fallback `name || id` que `UserForm.tsx` já usa nos botões de seleção) — antes mostrava o UUID cru, bug presente tanto aqui quanto em `UserDetailSettingsPage`.

Hooks customizados: `useEventSource(path, onMessage)` — abre um `EventSource` autenticado via `?token=` e chama `onMessage` a cada evento; `path = null` fecha sem abrir. `useStats()` — busca `/api/stats` com polling de 30 s. `useSettings()` / `useAbout()` — buscam `/api/settings` e `/api/about`.

```bash
make frontend # builda o frontend via Docker (node:20-alpine) — gera frontend/dist
cd frontend
yarn install  # apenas para desenvolvimento local do frontend
yarn dev      # Vite dev server na porta 5173 (proxy /api e /stream para :8080)
```

**Node/yarn não estão instalados no sistema host** — os testes e builds do frontend rodam via Docker. Para rodar os testes do frontend localmente use:

```bash
# Testes + lint + build do frontend via Docker (equivalente ao CI)
docker run --rm \
  --user "$(id -u):$(id -g)" \
  -v "$(pwd)/frontend":/app \
  -v camera-yarn-cache:/yarn-cache \
  -w /app \
  -e YARN_CACHE_FOLDER=/yarn-cache \
  -e HOME=/tmp \
  node:20-alpine \
  sh -c "yarn install --frozen-lockfile && yarn lint && yarn test --run && yarn build"
```

Nunca afirmar que os testes do frontend passaram sem ter rodado o comando acima (ou visto o CI verde).

### Serviço YOLO (`services/yolo/`)

Microserviço Python/FastAPI opcional para análise de gravações e fine-tuning. Expõe:
- `POST /analyze` — inferência YOLO em arquivo MP4
- `POST /finetune` / `GET /finetune/status/{id}` / `DELETE /finetune/{id}` — treino assíncrono (detecção)
- **State classification** (`yolov8n-cls`): `POST /classify` (imagem/crop → `{predictions:[{label,prob}], top}`), `POST /classify/train` (treina a partir de samples `{image_path,label}`, dataset em **pastas por classe**, assíncrono — status pelo mesmo `GET /finetune/status/{id}`; guard de tamanho barra `l`/`x`; treina com **`fliplr=0.0`** — o flip horizontal corromperia classes direcionais, ex.: pessoa entrando vs saindo), `GET /classify/models`.

**Testes do serviço:** `services/yolo/test_main.py` (pytest). As deps pesadas (torch/ultralytics/cv2) são **stubadas via `sys.modules`** antes de importar `main`, então os testes rodam numa imagem Python slim sem GPU. Rodam via `scripts/yolo-check.sh` (Docker), acionado pelo `scripts/check.sh` quando `services/yolo/` muda, e por um job dedicado no CI (`.github/workflows/ci.yml`).

**Subir o serviço:** totalmente independente do `docker-compose.yml` da câmera — compose file próprio em `services/yolo/`. Só precisa (1) estar acessível pela URL configurada em Settings e (2) montar o **mesmo diretório de storage** da instância de câmera que vai consumi-lo (os paths de arquivo trocados via API são resolvidos dentro do container do YOLO a partir desse volume — divergência aqui é a causa mais comum de `404` no `/classify`/`/analyze`). Copie `services/yolo/.env.example` para `services/yolo/.env` e ajuste `YOLO_STORAGE_PATH`/`YOLO_MODELS_PATH` antes de subir.

```bash
# CPU (qualquer hardware, incluindo Raspberry Pi)
docker compose -f services/yolo/docker-compose.yml up -d

# GPU NVIDIA (requer nvidia-container-toolkit no host)
docker compose -f services/yolo/docker-compose.yml -f services/yolo/docker-compose.nvidia.yml up -d
```

O padrão de **override files** mantém `services/yolo/docker-compose.yml` universal (funciona em RPi, AMD, CPU-only) e `services/yolo/docker-compose.nvidia.yml` adiciona apenas o device reservation NVIDIA. Nunca colocar configuração de GPU no `docker-compose.yml` base.

Modelos pré-baixados na imagem: `yolov8n` e `yolo11n`. Com GPU RTX 3050 (4GB VRAM): fine-tuning viável para variantes `n` e `s`; variantes `l` e `x` causam OOM no treino (inferência funciona). Ver `docs/analysis.md` para documentação completa.

### Testes e2e (`e2e/`)

Suíte Playwright ponta-a-ponta, **independente do resto do projeto**: pacote próprio (`package.json`/`tsconfig.json`/`eslint.config.js`/`.prettierrc`, gerenciado só por **bun** — o `frontend/` continua em yarn, sem relação) e **Docker-only** — tanto o servidor sob teste quanto o Playwright rodam em container, sem exigir Go/Node/bun no host além do Docker.

- `e2e/seed/` — programa Go standalone (`go build ./e2e/seed`) que gera um **fixture determinístico** direto via `internal/db`/`internal/config` (sem RTSP real). O dado (`Fixture`, `e2e/seed/fixture.go`) é separado de como é materializado (`applyFixture`): `Fixture{Users []FixtureUser; Cameras []FixtureCamera}` — `FixtureCamera` **embute** `config.CameraConfig` (`yaml:",inline"`, reaproveita as tags yaml que esse struct já tinha) + `Recordings int` (nº de gravações contíguas, `recordingSlots`/`schedule_test.go`) + `Events []FixtureMotionEvent` (referencia uma gravação da própria câmera por índice cronológico — `RecordingIndex`/`OffsetSeconds` — nunca um instante solto; `Score`/`Label`/`BboxX/Y/W/H` viram um `db.MotionEvent`). `defaultFixture(...)` é o fixture usado por `scripts/e2e.sh` (sem `-fixture`): 1 admin, 1 **viewer com acesso concedido só à 1ª câmera** (`db.SetUserCameras` — cobre o cenário de acesso restrito) e uma **2ª câmera nunca concedida** (cenário negativo — viewer tenta acessá-la e recebe "câmera não encontrada", já que `GET /api/cameras` filtra a lista por usuário). `-fixture <arquivo.yaml>` (opcional) troca o default por um arquivo no mesmo formato — útil pra câmeras com configs diversas ou eventos de movimento com label/bbox, que não escalariam como flag por dimensão (ver exemplo em `e2e/seed/fixtures/motion-example.yaml`). Cada gravação é uma cópia de um MP4 de amostra embutido (`sample.mp4`, via `go:embed`). Escreve o `camera.yaml` de bootstrap com caminhos **absolutos** (`cmd/camera` resolve `DBPath`/`Storage.Path` relativos ao CWD do processo, não ao diretório do yaml) e imprime os ids gerados em JSON (1º admin/viewer/câmeras do Fixture, mesma convenção de sempre). Ids/credenciais do fixture default (`-admin-user`, `-admin-pass`, `-camera-id`, `-admin-only-camera-id`, `-viewer-user`, `-viewer-pass`) são **flags**, não constantes — os defaults só valem quando a flag é omitida; quem efetivamente os define é o `docker-compose.yml` (ver abaixo), pra não duplicar os mesmos literais no código Go.
- Novo stage `e2e` no `Dockerfile` raiz (+`e2e-builder`, build nativo sem cross-compile) builda `camera`+`seed` numa imagem mínima com `ffmpeg`+`curl`; `e2e/docker-entrypoint.sh` repassa `E2E_ADMIN_USER`/`E2E_ADMIN_PASS`/`E2E_CAMERA_ID`/`E2E_ADMIN_ONLY_CAMERA_ID`/`E2E_VIEWER_USER`/`E2E_VIEWER_PASS` do ambiente como flags do seed (sem fallback aqui de propósito — ver `e2e/.env.example`), semeia o fixture num dir efêmero e sobe o servidor já apontado pra ele.
- `e2e/docker-compose.yml` — dois serviços numa rede só do compose (sem porta publicada no host): `e2e-camera` (build do stage `e2e`, healthcheck via `curl` em `/api/config`) e `e2e-playwright` (imagem `oven/bun:1`, instala os browsers na hora — `bunx playwright install --with-deps chromium` —, roda `bunx playwright test` contra `http://e2e-camera:8099`). Nomes com prefixo `e2e-` (convenção do projeto pra serviços não-produção — agrupa por contexto, evita colisão com o `camera` de produção do `docker-compose.yml` raiz quando este arquivo é incluído nele, ver abaixo). Ids/credenciais do fixture (câmera com UUID fixo, `recording_id=1`) chegam aos dois serviços via um anchor YAML único (`x-fixture-env`, mesclado com `<<:` no `environment:` de cada serviço) — **única declaração** desses valores no repo, com sintaxe `${VAR:-default}` pra permitir override via `e2e/.env` (ver `e2e/.env.example`) sem editar o compose. As opções do Playwright (`reporter`/`screenshot`/`video`, `e2e/playwright.config.ts`) são configuráveis do mesmo jeito, só no serviço `e2e-playwright`.
- **Iterar num spec e2e sem pagar o custo de build+seed+install do zero a cada vez:** o `docker-compose.yml` raiz inclui `e2e/docker-compose.yml` via `include:` e sobrescreve `e2e-camera`/`e2e-playwright` com `profiles: [development]` (opt-in, mesmo profile de `dev-camera` — nenhum `docker compose up`/`config` sem `--profile development` enxerga esses dois serviços). O override de `e2e-playwright` troca o `command:` one-shot original (usado só por `scripts/e2e.sh`/CI) por um keep-alive: instala deps do bun + Chromium uma vez e fica idle (`tail -f /dev/null`); daí um teste roda repetidamente via `docker compose --profile development exec e2e-playwright bunx playwright test <arquivo>`, sem reinstalar nada. Volume nomeado próprio (`e2e-playwright-browsers`) persiste o cache do Chromium (`~/.cache/ms-playwright`) entre subidas — sem ele, cada `docker compose up` reinstalaria o browser do zero. `scripts/compose-check.sh` valida essa topologia via `docker compose config` de verdade (não leitura de texto): `e2e-camera`/`e2e-playwright` só aparecem com `--profile development`, nunca sem. **Suba com `--profile development up -d --wait` antes do 1º `exec`** (ou espere `docker compose ps` mostrar `(healthy)`) — o `command:` do `e2e-playwright` só cria o marcador `/tmp/e2e-playwright-ready` (o que o `healthcheck:` observa) depois que `bun install`+`playwright install` terminam; um `exec` disparado antes disso falha ("Executable doesn't exist").
- `scripts/e2e.sh` — orquestrador estático (`docker compose up --build --abort-on-container-exit --exit-code-from e2e-playwright`, teardown via `trap`), mesmo padrão de `scripts/frontend-check.sh`/`scripts/yolo-check.sh`. Rodado por um job dedicado no CI (`.github/workflows/ci.yml`, job `e2e`), **bloqueante em todo PR** (sem filtro de path) — deliberadamente enxuto (specs de smoke pro papel admin e um cenário de acesso restrito pro papel viewer, não escala/performance), já que `scripts/merge-when-green.sh` espera todos os check-runs antes de mergear.
- `scripts/e2e.sh` gera `e2e/playwright-report/` (HTML, gitignored). `scripts/e2e-report.sh [porta]` sobe um container `oven/bun:1` (mesma imagem do serviço `e2e-playwright`) rodando `bunx playwright show-report --host 0.0.0.0`, com a porta publicada no host (default `9323`, diferente do compose principal que não publica porta nenhuma) — só pra essa visualização pontual, sem exigir bun no host.
- **Relatório em PDF (opcional):** `E2E_PDF_REPORT=on` (default `off`, ver `e2e/.env.example`) acrescenta o reporter customizado `e2e/reporters/pdf-reporter.ts` ao array de `reporter` em `e2e/playwright.config.ts` — aditivo ao `E2E_REPORTER` (HTML/list/etc.), não o substitui. Existe porque o relatório HTML oficial do Playwright é uma SPA com testes colapsados e screenshots atrás de lazy-load — um print direto da página captura só a lista resumida, sem detalhes nem imagens. O reporter monta um HTML "plano" (já expandido, título/status/duração/erros por teste) com cada screenshot embutido inline via `data:image/png;base64,...` (lido do path do attachment) e imprime esse HTML com `chromium.launch()` + `page.pdf()` — os bytes da imagem ficam nos próprios objetos `/Image` do PDF, nunca uma referência externa a `playwright-report/data/`. Gera `e2e/playwright-report/report.pdf` (mesmo diretório do HTML, gitignored, sobrescrito a cada run).

## Arquitetura

### Binários

| Binário | Responsabilidade |
|---|---|
| `cmd/camera` | Servidor principal: grava, faz streaming HLS, detecta movimento e serve a SPA. Suporta o subcomando `camera init` — wizard interativo que gera o arquivo de bootstrap (`camera.yaml`) com porta, `db_path`, storage e credenciais do admin inicial. |
| `cmd/mcp-ffprobe` | Servidor MCP (stdio) que expõe `probe_stream` — executa ffprobe em uma URL RTSP e retorna os metadados JSON do stream. Útil para inspeção de câmeras via ferramentas MCP. |

### Fluxo de inicialização (`cmd/camera/main.go`)

1. Lê o arquivo de bootstrap (`camera.yaml`) com porta, `db_path`, storage e credenciais do admin.
2. Abre o banco SQLite e executa as migrations pendentes.
3. Na primeira execução, cria o usuário admin com `must_change_password = true`.
4. Lê câmeras do banco e para cada câmera habilitada inicia:
   - Um `recorder.Recorder` — grava RTSP/HLS → MP4 chunk (somente se `recording_enabled=true`)
   - Um `hls.HLSStreamer` — grava RTSP/HLS → segmentos HLS para live (gateado por `webrtc.ShouldRunHLS`)
   - Um `webrtc.Publisher` — entrega o ao-vivo de baixa latência (só câmeras H.264, gateado por `webrtc.ShouldPublish`)
   - Um `motion.Monitor` — detecta movimento via ffmpeg pipe raw (se motion habilitado)
5. O `server.Server` é levantado em goroutine separada e serve a SPA + API REST; um `notifications.Dispatcher` é construído e injetado nele e no `storage.Cleaner`.
6. Um `stateengine.Runner` por classificador de estado habilitado sobe quando o serviço YOLO está configurado.
7. Câmeras adicionadas/removidas via API ativam callbacks `onCameraStart` / `onCameraStop` (em goroutine, para não bloquear o handler HTTP).

### Pacotes internos

| Pacote | Responsabilidade |
|---|---|
| `internal/core` | Utilitários de captura genéricos compartilhados por mais de um protocolo. Doc completa: [docs/go-modules/internal/core/README.md](docs/go-modules/internal/core/README.md). |
| `internal/capturer` | Builders de argumentos ffmpeg por protocolo de captura (rtsp/hls). Doc completa: [docs/go-modules/internal/capturer/README.md](docs/go-modules/internal/capturer/README.md). |
| `internal/capturer/rtsp` | Args de conexão/snapshot RTSP. Doc completa: [docs/go-modules/internal/capturer/rtsp/README.md](docs/go-modules/internal/capturer/rtsp/README.md). |
| `internal/capturer/hls` | Args de conexão pra câmeras cuja fonte já é HLS. Doc completa: [docs/go-modules/internal/capturer/hls/README.md](docs/go-modules/internal/capturer/hls/README.md). |
| `internal/exec` | Interfaces `Commander`/`Process` (processo ffmpeg de longa duração) sobre `os/exec`, injetadas nos pacotes de captura pra permitir testes sem ffmpeg. Doc completa: [docs/go-modules/internal/exec/README.md](docs/go-modules/internal/exec/README.md). |
| `internal/recorder` | Grava RTSP/HLS em chunks MP4 não fragmentados. Doc completa: [docs/go-modules/internal/recorder/README.md](docs/go-modules/internal/recorder/README.md). |
| `internal/transmission/hls` | Gera playlist HLS ao vivo (modo padrão ou DVR). Doc completa: [docs/go-modules/internal/transmission/hls/README.md](docs/go-modules/internal/transmission/hls/README.md). |
| `internal/transmission/webrtc` | Entrega o ao-vivo de baixa latência via WebRTC (sub-segundo, só H.264), par simétrico do HLS. Doc completa: [docs/go-modules/internal/transmission/webrtc/README.md](docs/go-modules/internal/transmission/webrtc/README.md). |
| `internal/motion` | Detecta movimento via ffmpeg pipe raw; persiste eventos e snapshot anotado. Doc completa: [docs/go-modules/internal/motion/README.md](docs/go-modules/internal/motion/README.md). |
| `internal/zones` | Tipo `Zone` (zonas de exclusão/detecção), sem lógica de detecção. Doc completa: [docs/go-modules/internal/zones/README.md](docs/go-modules/internal/zones/README.md). |
| `internal/discovery` | Descoberta de câmeras na rede (ONVIF WS-Discovery + varredura de porta 554). Doc completa: [docs/go-modules/internal/discovery/README.md](docs/go-modules/internal/discovery/README.md). |
| `internal/storage` | `Cleaner`: retenção diferenciada por categoria, sincronização filesystem↔banco, S3, aviso de disco cheio. Doc completa: [docs/go-modules/internal/storage/README.md](docs/go-modules/internal/storage/README.md). |
| `internal/db` | Acesso ao SQLite (`modernc.org/sqlite`); migrations em `internal/db/migrations/` (doc: [docs/go-modules/internal/db/migrations/README.md](docs/go-modules/internal/db/migrations/README.md)); tabelas de câmeras/usuários/gravações/eventos/state classification; `user_settings` (KV genérico por usuário). Doc completa: [docs/go-modules/internal/db/README.md](docs/go-modules/internal/db/README.md). |
| `internal/dbbackup` | Snapshot/restore do banco SQLite (rede de segurança do updater). Doc completa: [docs/go-modules/internal/dbbackup/README.md](docs/go-modules/internal/dbbackup/README.md). |
| `internal/email` | Envio de e-mail (recuperação de senha; genérico o bastante pra outros usos, ex. sender `email` de notificações). Doc completa: [docs/go-modules/internal/email/README.md](docs/go-modules/internal/email/README.md). |
| `internal/notifications` | Único ponto da aplicação que sabe como entregar uma notificação (application/email hoje); quem sabe pra quem continua sendo o chamador. Doc completa: [docs/go-modules/internal/notifications/README.md](docs/go-modules/internal/notifications/README.md). |
| `internal/notifications/application` | Sender "dentro da aplicação" (persistência + push ao vivo). Doc completa: [docs/go-modules/internal/notifications/application/README.md](docs/go-modules/internal/notifications/application/README.md). |
| `internal/notifications/email` | Sender por e-mail, opt-in por usuário. Doc completa: [docs/go-modules/internal/notifications/email/README.md](docs/go-modules/internal/notifications/email/README.md). |
| `internal/ffprobe` | Executa e parseia saída JSON do ffprobe para detectar codec, áudio e dimensões do stream. Doc completa: [docs/go-modules/internal/ffprobe/README.md](docs/go-modules/internal/ffprobe/README.md). |
| `internal/deviceinfo` | Captura metadados de hardware/manutenção da câmera (EAV, extensível por `Collector` — hoje só Dahua/Intelbras). Doc completa: [docs/go-modules/internal/deviceinfo/README.md](docs/go-modules/internal/deviceinfo/README.md). |
| `internal/server` | HTTP server: JWT + API REST + arquivos de gravação/HLS + a SPA React embutida. Doc completa: [docs/go-modules/internal/server/README.md](docs/go-modules/internal/server/README.md). |
| `internal/release` | Checagem de atualização disponível e busca de changelog (GitHub Releases). Doc completa: [docs/go-modules/internal/release/README.md](docs/go-modules/internal/release/README.md). |
| `internal/updater` | Detecta o modo de auto-atualização do ambiente e aplica (self-replace/docker/notify). Doc completa: [docs/go-modules/internal/updater/README.md](docs/go-modules/internal/updater/README.md). |
| `internal/extensions` | Domínio de integrações opcionais ("extensões"), configuráveis em Preferências > Extensões; agrupa um subpacote por extensão. Doc completa: [docs/go-modules/internal/extensions/README.md](docs/go-modules/internal/extensions/README.md). |
| `internal/extensions/telegram` | Cliente mínimo da Bot API do Telegram — pacote autocontido, não invocado por `main.go` ainda (ativação em Preferências > Extensões não dispara nada nesta história). Doc completa: [docs/go-modules/internal/extensions/telegram/README.md](docs/go-modules/internal/extensions/telegram/README.md). |
| `internal/trainer` | Despacha jobs de fine-tuning de object detection pra um adapter de backend plugável (`trainers.type`). Doc completa: [docs/go-modules/internal/trainer/README.md](docs/go-modules/internal/trainer/README.md). |
| `internal/trainer/adapters` | O adapter `Yolo`, único backend de treino hoje. Doc completa: [docs/go-modules/internal/trainer/adapters/README.md](docs/go-modules/internal/trainer/adapters/README.md). |
| `internal/analysis` | Cliente HTTP do serviço YOLO — tipos/Client compartilhados por detecção, state classification e fine-tuning. Doc completa: [docs/go-modules/internal/analysis/README.md](docs/go-modules/internal/analysis/README.md). |
| `internal/detector` | Despacha inferência de detecção de objetos pra um adapter plugável (yolo/huggingface). Doc completa: [docs/go-modules/internal/detector/README.md](docs/go-modules/internal/detector/README.md). |
| `internal/detector/adapters` | Os adapters `Yolo` e `HuggingFace`. Doc completa: [docs/go-modules/internal/detector/adapters/README.md](docs/go-modules/internal/detector/adapters/README.md). |
| `internal/stateclass` | Tipos de domínio da classificação de estado (`Classifier`, `Tracker`). Doc completa: [docs/go-modules/internal/stateclass/README.md](docs/go-modules/internal/stateclass/README.md). |
| `internal/stateengine` | Roda a inferência de estado: grab → classify → tracker → persist/emit. Doc completa: [docs/go-modules/internal/stateengine/README.md](docs/go-modules/internal/stateengine/README.md). |
| `internal/config` | Lê o arquivo de bootstrap (`camera.yaml`) com porta, `db_path`, storage e credenciais do admin. Variáveis de ambiente sobrescrevem campos específicos (ver abaixo). Doc completa: [docs/go-modules/internal/config/README.md](docs/go-modules/internal/config/README.md). |
| `internal/logger` | Constrói o `*slog.Logger` a partir de `config.LogConfig` — `stdout` (JSON) ou `file` (um arquivo por nível, com rotação via lumberjack). Knobs em `camera.yaml`, seção `log:`. Doc completa: [docs/go-modules/internal/logger/README.md](docs/go-modules/internal/logger/README.md). |
| `frontend/` | SPA React/Vite/Tailwind embutida via `go:embed all:dist`. `ChangePasswordPage` — tela obrigatória no primeiro login; bloqueia acesso ao restante da UI enquanto `must_change_password=true` no JWT. |

### Superfície da API

A **fonte de verdade das rotas** é `internal/server/routes.go` → `routeTable()`: uma tabela declarativa de `route{method, path, auth, handler}`. O `routes()` itera essa tabela e aplica o middleware **derivado do `authLevel`** (`authPublic`/`authChangePassword`/`authFull`/`authAdmin`/`authCamera` → `guard()`), em vez de embrulhar cada rota à mão. Só os mounts por prefixo (`/stream/`, `/recordings/`) e o `spaHandler` ficam fora da tabela (são `http.Handler`, não `http.HandlerFunc`).

Erros da API são `text/plain` (via `http.Error`), não um envelope JSON — clientes devem programar contra o status, não contra o texto do erro.

### Autenticação

O JWT é assinado com um segredo aleatório gerado no boot — tokens não sobrevivem a reinicializações do servidor. O token é aceito via header `Authorization: Bearer <token>` ou query param `?token=<token>` (necessário para `<video src>` e `<Player>`).

Fluxo de primeiro acesso: o admin inicial é criado com `must_change_password = true`. No primeiro login o servidor emite um token com esse claim; o frontend redireciona obrigatoriamente para `ChangePasswordPage`. Após a troca a flag é zerada no banco e o acesso normal é liberado. A senha do bootstrap não precisa ser atualizada — serve apenas na criação inicial.

### Build info

`version`, `commit` e `builtAt` são injetados via `-ldflags` no `Makefile`. Em `main.go` são passados ao servidor via `WithVersion(version)` e `WithBuildInfo(commit, builtAt)`. O endpoint `GET /api/about` expõe esses valores junto com `uptime_seconds` e `go_version`.

## Variáveis de ambiente

Todas seguem o prefixo `OS_CAMERA_`.

| Variável | Campo sobrescrito |
|---|---|
| `OS_CAMERA_TIMEZONE` | `timezone` (fuso da instalação; usado pelo servidor para interpretar datas locais) |
| `OS_CAMERA_JWT_SECRET` | `server.jwt_secret` (segredo JWT fixo; vazio = gerado aleatoriamente a cada boot) |
| `OS_CAMERA_DEBUG` | `debug` (ativa logs de nível debug) |
| `OS_CAMERA_SMTP_HOST` | `smtp.host` |
| `OS_CAMERA_SMTP_PORT` | `smtp.port` |
| `OS_CAMERA_SMTP_USERNAME` | `smtp.username` |
| `OS_CAMERA_SMTP_PASSWORD` | `smtp.password` |
| `OS_CAMERA_SMTP_FROM_NAME` | `smtp.from_name` (nome de exibição do remetente; default `"os-camera"`) |
| `OS_CAMERA_SMTP_FROM_EMAIL` | `smtp.from_email` (e-mail do remetente/envelope `MAIL FROM`; default = `smtp.username`) |
| `OS_CAMERA_STORAGE_PATH` | `storage.path` (diretório raiz das gravações) |

`smtp.*` é configuração de conexão (`internal/config.SMTPConfig`); o envio em si é feito por `internal/email` (ver tabela de pacotes internos acima). `FromName`/`FromEmail` resolvem seus defaults (`"os-camera"`/`Username`) em `internal/email.SMTPSender.Send`, não em `Load()` — os campos ficam vazios em `SMTPConfig` quando não configurados.

## Diretório `work_progress/`

Diretório único (gitignored: `work_progress/`) que agrupa todo o estado de trabalho não versionado do fluxo — cada um dos quatro subdiretórios abaixo mantém seu próprio propósito e convenções, só a raiz mudou:

- `work_progress/analysis/` — análises da fase G1 (`/analyze`), ver "Fluxo de trabalho" acima e `docs/workflow.md`.
- `work_progress/stories/` — a história atual decomposta em tickets (fase G2, `/story`), idem.
- `work_progress/releases/` — planejamento/corte de release (`_next.md` e releases publicadas), idem.
- `work_progress/amostras/` — reservado para arquivos que o navigator coloca para análise contextual — screenshots, logs, dumps de banco, exemplos de vídeo ou qualquer artefato que ajude a diagnosticar um problema. Claude deve inspecionar o conteúdo desse diretório quando o navigator mencionar que colocou algo lá, ou quando precisar de evidência concreta para uma investigação.

## Manutenção contínua

- **Decisões de fluxo se registram em `docs/workflow.md`** — única fonte canônica do processo (ver "Fluxo de trabalho" acima). A memória do Claude é só atalho/ponteiro: nunca deixe uma regra de workflow apenas na memória.
- **Ao adicionar ou alterar qualquer funcionalidade**, revise este `CLAUDE.md` e atualize as seções afetadas.
- **Ao adicionar, remover ou mudar o acesso de uma rota**, atualize `internal/server/routes.go` (a tabela).
- **Ao adicionar ou alterar qualquer campo de configuração**, atualize `camera.yaml.example` com o novo campo, valor de exemplo e comentário com a variável de ambiente correspondente (se houver).

## Convenções de teste

Testes usam `httptest.NewRecorder` (server), `fakeCommander` com `trackingProcess` (recorder/streamer) e implementações fake das interfaces de `internal/exec`. O banco SQLite é criado em memória (`:memory:`) nos testes de server e db — nenhum mock externo. Cada pacote é testado em isolamento via injeção de dependência.

## Diretrizes de Desenvolvimento Go

Sempre priorize a simplicidade e a legibilidade conforme os provérbios do Go ("Effective Go").

### 1. Princípio DRY e Abstração
- **Evite Abstração Precoce:** Siga a "Regra de Três". Não crie abstrações ou interfaces até que haja pelo menos três casos de uso concretos.
- **Cópia vs. Dependência:** Prefira duplicar uma pequena função utilitária do que introduzir uma dependência externa desnecessária.
- **Interfaces:** Defina interfaces no lado do consumidor (onde são usadas) e não no lado do produtor. Mantenha as interfaces pequenas (1 ou 2 métodos).

### 2. Estilo e Convenções de Código
- **Alinhamento do "Happy Path":** Mantenha o fluxo principal de sucesso alinhado à esquerda. Use *guard clauses* para tratar erros e retorne o mais cedo possível.
- **Nomenclatura:**
    - Variáveis de escopo curto: Curtas (ex: `ctx`, `w`, `r`, `i`).
    - Variáveis globais/longas: Descritivas.
    - Interfaces: Sufixo "-er" para interfaces de um único método (ex: `Formatter`, `Storer`).
- **Zero Value:** Projete structs para que o valor zero (`var s MyStruct`) seja útil e seguro para uso imediato.

### 3. Tratamento de Erros
- **Erros são Valores:** Sempre verifique erros explicitamente logo após a chamada: `if err != nil { return err }`.
- **Contexto de Erro:** Utilize `fmt.Errorf("contexto do erro: %w", err)` para adicionar contexto sem perder o erro original (wrapping).
- **Sem Panics:** Nunca use `panic` para controle de fluxo. Reserve-o apenas para erros catastróficos de inicialização ou bugs lógicos irrecuperáveis.

### 4. Concorrência e Performance
- **Canais vs. Mutex:** "Não comunique compartilhando memória; compartilhe memória comunicando". Use canais para orquestração e `sync.Mutex` para proteção de estado simples.
- **Goroutines:** Sempre saiba como uma goroutine vai terminar antes de iniciá-la para evitar vazamentos de memória.
- **Ponteiros:** Use ponteiros apenas quando precisar mutar o estado ou para evitar cópias de structs muito grandes (> 64-128 bytes).

### 5. Tooling Obrigatório
- Todo código gerado deve ser compatível com o `gofmt`.
