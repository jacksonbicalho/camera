{
  "name": "os-camera-e2e",
  "private": true,
  "description": "End-to-end (Playwright) harness for os-camera. Run via scripts/e2e.sh.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.49.1"
  }
}
